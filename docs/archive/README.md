# Archive — superseded point-in-time reports (DOC-001)

These `FINAL_*` snapshots were written during earlier milestones and
contradict each other and the current tree (e.g. some predate the circuit
breaker, the router split, MFA and the usage ledger). They are kept for
history only — **do not quote them as current status**.

Single sources of truth now:

- `../architecture.md` — verified decisions + pending items
- `../../BANGLA_GPT_ENTERPRISE_ARCHITECTURE_QA_AUDIT.md` — enterprise
  architecture + full QA audit (evidence-driven, reproducible)
- `../API.md` — hand-written endpoint reference
- `../runbook.md` — operations runbook

New milestone reports go beside those files, never as another `FINAL_*`.
