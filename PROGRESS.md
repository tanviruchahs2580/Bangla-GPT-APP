# PROGRESS — Bangla GPT Master Roadmap v1.0

**Last updated:** 2026-09-08 — **Current:** Phase H v0.5.0 release finalization DONE (live fresh + full user-mode E2E + 4 real bugs fixed — BUG-1/2 found by live testing, BUG-3 by the local docker gate, BUG-4 by GitHub CI's Postgres engine check (FK-incomplete account deletion, invisible to local sqlite) + local CI matrix all-green: pytest 492 passed + 6 skipped exit 0, redis-gated 3 proven green against real Redis; docker ENV=ci smoke green after package-data fix BUG-3; Postgres engine check re-proven green locally on a throwaway postgres:16). No-commit directive LIFTED by user 2026-09-08 for the release train (commit+push+tag v0.5.0 authorized). Open: S6.1 (NCTB PDFs + permission, ✍), S6.7 sign-off (✍), G6 staging legs (✍)
**Gates:** [x] G0 (code) · [x] G1 · [x] G2 · [x] G3 (local; staging legs ✍) · [~] G4 (golden+router legs unconditional PASS; real-model TTFB c = app-side PASS, model-side pending staging ✍) · [x] G5 (local; legal/staging ✍) · [ ] G6 · [ ] G7
**Human inputs:** GEMINI_API_KEY ✅ done (live) · Staging server pending · Sentry DSN pending (code done, live pending) · Postgres/Redis/SMTP/NCTB/Android/School pending

## Stages & Steps (64)
- [x] **0.1 Real LLM provider (Gemini) live** | status: done ✅ LIVE VERIFIED | evidence: pooled AsyncClient + latency logging; live /ready → gemini, POST /tutor/ask কোষ কী? → grounded:true, 3 sources, answer 49 chars no leak, latency 4129ms logged via json_log; key stored in apps/api/.env (gitignored, never hardcoded) per R7; pytest 173 passed | commit: 5e6ff4f
- [x] **0.2 Mock leak fix (AUD-01)** | status: done | evidence: mock.py already returns [mock] quoted evidence (no system); new test test_mock_never_leaks_system_prompt asserts no <evidence>/<user_question>/AUD-01 tokens; pytest 172 passed | commit: eb2955d
- [x] **0.3 Markdown + KaTeX rendering** | status: done | evidence: SafeMarkdown (react-markdown+remark-math+rehype-katex, XSS regex strip, katex CSS) wired into AITutorPage bubble + LearnChapterPage sections; vitest 10 passed (4 files) includes KaTeX+XSS tests; tsc clean; vite build green (katex chunk 392kB) | commit: 48bbabb
- [x] **0.4 Postgres migration** | status: done | evidence: engine now pool_pre_ping+pool_size=10 for postgres; alembic upgrade head d4e5f6a7b8c9 on pg16 (5433), downgrade -1 ↔ upgrade reversible, pytest test_postgres_smoke PASSED, full suite 172 passed; scripts/pg_backup.sh created + drill executed (docker pg) | commit: a4ae70a
- [x] **0.5 Config hardening** | status: done | evidence: enforce_production_safety now checks gemini key + CORS allowlist (no wildcard, must be set); negative test test_production_safety_rejects_weak_config covers JWT/Gemini/CORS; ruff/mypy clean | commit: 060e5e4
- [x] **0.6 Observability baseline** | status: done | evidence: sentry-sdk + SENTRY_DSN/SENTRY_ENV config, Sentry init no-op when DSN absent, request_id_var contextvar propagated into json_log + middleware, Grafana dashboard deploy/grafana/dashboards/bangla-gpt.json, web @sentry/react init, tests for request_id+ sentry noop pass, ruff/mypy/tsc/vitest/build green | commit: ff81159
- [x] **0.7 Real deploy + automated smoke** | status: done (script) | evidence: scripts/smoke.sh created (health→register→login→learn→tutor grounded→quiz→me→export→delete, local smoke via python verified, rollback doc in script); staging deploy via release.yml pending server 🖐 | commit: fcf5f7f
- [x] **1.1 /dashboard/summary + Continue + Recommendation v1** | status: done | evidence: GET /dashboard/summary returns {user,today,continue_learning,quick_actions,recommendation,progress}; HomePage shows চালিয়ে যান card (localStorage fallback) + weak mini-quiz recommendation; pytest 3 dashboard tests pass; ruff/mypy/tsc/vitest/build green | commit: b17a2de
- [x] **1.2 Learn progress + Bookmark + TTS + font slider** | status: done ✅ VERIFIED (2026-09-06; found+fixed real bug: ChapterProgress imported only locally in POST but referenced in GET → NameError; added top-level import) | evidence: migration f1a2b3c4d5e6 reversible (upgrade→downgrade -1→upgrade head, table present); pytest tests/test_learn_progress.py 6 green (upsert create / partial-update preserves fields / subject filter / read_pct 422 / teacher 403 / GET empty for teacher); badge+TTS logic extracted to lib/progress.ts, vitest 6 green; TTS bn-BD feature-guarded; font slider persisted; tsc clean. Commit withheld per user no-commit directive | commit: — (no-commit directive)
- [x] **1.3 Unified Learning Workspace** | status: done ✅ VERIFIED (2026-09-06) | evidence: chapter context threaded end-to-end — bm25.search(chapter=), tutor.ask/ask_stream/_retrieve (chapter-scoped with gate-aware fallback), quiz generate(chapter=), schemas AskRequest/ChatSendRequest/QuizStartRequest.chapter, main.py passes payload.chapter at 5 call sites; pytest tests/test_workspace.py 3 green (quiz preselect / tutor grounded in chapter / out-of-scope fallback); frontend LearnChapterPage tab bar (পড়়া/অনুশীলন/জিজ্ঞাসা, role=tablist) + ChapterPractice + ChapterAsk with context chip; i18n bn+en keys; vitest 19 green (3 new in workspace.test.tsx); tsc/ruff/mypy clean; full pytest 184 passed 3 skipped | commit: — (no-commit directive)
- [x] **1.4 Structured AI response** | status: done ✅ VERIFIED (2026-09-06) | evidence: SYSTEM_PROMPT rule ৬ (সহজ ব্যাখ্যা→উদাহরণ→মূল বিষয় bullets→তুমি বুঝেছ?) + SECTION_* constants in services/answer_structure.py (import-cycle free); mock provider emits deterministic golden layout; pytest tests/test_structured_answer.py 4 green (prompt declares sections / grounded answer all sections in order / refusal stays plain / no prompt leak) + test_health mock-format assertion updated (still asserts no system/markup leak); frontend lib/structuredAnswer.ts parser (byte-identical labels, bullet-tolerant, <3 labels→plain fallback), AssistantContent structured bubble, 3 quick chips (সহজ/উদাহরণ→follow-up send, কুইজ→/student/quiz?subject= preselect via useSearchParams); i18n bn+en; styles.css section styles (tokens only); vitest 23 green (+4 parser); tsc/ruff/mypy clean | commit: — (no-commit directive)
- [x] **1.5 "আমি বুঝিন" adaptive re-teach** | status: done ✅ VERIFIED (2026-09-06) | evidence: services/teach_strategy.py 5-strategy cycle (সহজ→বাস্তব উদাহরণ→ভিজ্যুয়া→গল্প→ধাপে ধাপে→wrap); Conversation.last_strategy via migration a2c5e7b9d1f3 (upgrade→downgrade→upgrade verified + PRAGMA present/absent column check); ChatSendRequest.reteach → reteach_instruction injected ahead of evidence block in tutor.ask/ask_stream (same failure → DIFFERENT strategy named — spec PASS); ConversationOut.last_strategy exposed; pytest tests/test_reteach.py 3 green; frontend "আমি বুঝিন" chip on grounded replies re-sends question with reteach=true; i18n bn+en; ruff/mypy(46)/tsc/vitest 23 green | commit: — (no-commit directive)
- [x] **1.6 Source → evidence modal** | status: done ✅ | evidence: backend SourceRef.excerpt via sanitize_evidence in tutor._sources; test_evidence_modal.py 2 green (excerpt >10 chars, no <evidence>/<user_question> markup, book/chapter path); frontend source chips → EvidenceModal (role=dialog, breadcrumb book›chapter›section·page, blockquote sanitized excerpt, Escape/backdrop/close); i18n bn+en keys; CSS append-only; vitest evidenceModal.test.tsx 2 green (suite 25/25); gates: pytest 196+3skip, ruff, mypy 46 files, tsc clean | commit: — (no-commit directive)
- [x] **1.7 Quiz explain loop** | status: done ✅ | evidence: AskRequest.explain (QuizExplainContext question/options/correct_index/user_answer/chapter) → quiz_explain_instruction injected as trusted extra_instruction + retrieval merged with quiz topic; QuizPage review per-wrong-item বুঝিযে দাও → tutor auto-ask via quizExplain.ts payload + back-link /student/quiz?result=1 (sessionStorage restore); test_quiz_explain.py 4 green (unit + e2e quiz→explain grounded); vitest quizExplain.test.ts 3 green (suite 28/28); smoke.sh explain step added; gates: pytest 200+3skip, ruff, mypy 47 files, tsc clean | commit: — (no-commit directive)
- [x] **1.8 History search + rename/delete** | status: done ✅ | evidence: PATCH /tutor/conversations/{id} (rename), DELETE (cascade messages), GET /tutor/messages/search?q= (own-history LIKE w/ escaped wildcards, snippet, limit 25); test_chat_history_crud.py 5 green (rename persists, validation, delete removes messages, cross-student isolation 403+no-leak, wildcard/short-query rules); UI: search box + hits, inline rename, delete w/ confirm — chips refetch (historyUi.test.tsx 2 green, suite 30/30); gates: pytest 205+3skip, ruff, mypy 47 files, tsc clean | commit: — (no-commit directive)
- [x] **1.9 Streak + activity heatmap** | status: done ✅ | evidence: DailyActivity model + migration b3d7f9a1c2e4 (upgrade/downgrade -1/upgrade reversible, verified on sqlite); services/activity.py Asia/Dhaka day boundary (ZoneInfo + fixed +06 fallback), streak ends-yesterday rule, zero-filled heatmap; record_activity wired on chat turns (both endpoints) + graded quiz submit; GET /students/{id}/activity (authorize_student_access, days 7-370 default 91); test_streak.py 9 green (UTC 18:30 -> next-day Dhaka, naive-as-UTC, streak chain/gap/yesterday rules, heatmap zero-fill oldest-first, endpoint records 1q+1quiz+2min+streak 1, window validation, cross-student 403); MePage GitHub-style 91-cell grid + streak card; i18n bn+en keys (codepoint-verified); CSS append-only .heatmap/.heat-cell tokens-only (byte-checked); vitest activityHeatmap.test.tsx 2 green (91 cells, level mapping, tooltip counts, query path; suite 32/32); gates: pytest 214+3skip, ruff, mypy 48 files, tsc, vite build green | commit: — (no-commit directive)
- [x] **1.10 Spaced revision (SM-2-lite)** | status: done ✅ | evidence: RevisionItem model + migration e6f7a8b9c0d1 (chained after b3d7f9a1c2e4, upgrade/downgrade -1/upgrade reversible on sqlite, table absent after downgrade); services/revision.py SM-2-lite: success 1/3/7d then interval*ease, failure resets reps and re-dues today, ease floor 1.3, Dhaka day arithmetic; quiz submit records every review item (correct -> +1d, wrong -> due today); GET /revision/due (due_count + items, limit 1-50), POST /revision/{id}/review (ownership 404, chosen -1..7 validation); test_revision.py 6 green (unit: 1/3/7 progression, growth past 7, failure reset, ease floor, quality clamp; e2e: due_count == wrong items, review reschedules to +1d and drops from due list, re-fail re-dues today, cross-student 404, 422 range); QuizPage revision tab w/ due badge + option-answer cards refetch; i18n bn+en (codepoint-verified); vitest revisionTab.test.tsx 2 green (badge count, POST+refetch; suite 34/34); gates: pytest 220+3skip, ruff, mypy 49 files, tsc, vite build green | commit: — (no-commit directive)
- [x] **1.11 Global search v1** | status: done ✅ | evidence: GET /search?q= (auth, 1-100 chars, blank 422) reuses create_app BM25 index: top-24 hits min_score 0.5, deduped to kind=subject (slug/book-title match) + kind=chapter (per subject+chapter+class, href /student/learn/{subject}/{chapter}?class=N quoted) + kind=question (interrogative section titles like 'kosha ki'; chapter entry suppressed when its question hit already links there); question-like queries (ending '?' or interrogative tokens ki/kii/keno/... written as backslash-u escapes, ASCII-safe source) set ask_action for the Tutor-ask action; test_search.py 8 green (relevance grounding to science cell hits, question-section + ask_action, plain keyword not question-like, ascii '?' sets ask, subject slug, nonsense query 0 hits, 401 unauth, 422 bounds); UI: SearchBox in AppShell topbar (student-only, 300ms debounce, click-outside/Escape close, ask-in-Tutor option), AITutorPage sends location.state.ask once (S1.7 explain pattern); i18n bn+en keys (codepoint-verified); CSS append-only .searchbox/.search-dropdown (hyphen byte-checked); vitest searchBox.test.tsx 4 green (debounce query, navigate to href, ask navigates /student/tutor with state, empty state; suite 38/38); gates: pytest 228+3skip, ruff, mypy 49 files, tsc, vite build green | commit: — (no-commit directive)
- [x] **1.12 Voice input (bn-BD)** | status: done ✅ | evidence: lib/voice.ts feature-detects SpeechRecognition/webkitSpeechRecognition (typed SpeechRecognitionLike, no DOM-lib hacks); components/VoiceButton.tsx hides itself entirely when unsupported, sets lang bn-BD + single alternative + no interim, transcript appended to composer, listening state via aria-pressed + voice-pulse CSS, mic error/no-speech/denied silently ends (composer typing unaffected), abort on unmount; mounted in AITutorPage composer between input and send; i18n voiceMic bn+en (codepoint-verified 'maike bolun'); vitest voiceInput.test.tsx 3 green (unsupported->renders nothing no crash, fake recognizer captures bn-BD + trimmed transcript callback + stop toggles aria-pressed, onerror no throw; suite 41/41); tsc + vite build green, backend untouched (pytest unchanged 228+3skip). MANUAL NOTE for final report: real mic capture + true bn-BD recognition needs a Chromium browser on a real device (jsdom cannot synthesize audio); unsupported-browser path is the automated PASS 🖐 | commit: — (no-commit directive)
- [x] **1.13 Offline download + low-data mode** | status: done ✅ | evidence: Backend: SHORT_ANSWER_INSTRUCTION in answer_structure (shared prompt truth; repaired real on-disk Bengali corruption of its leading token this session -- numeric dump showed the wrong codepoint sequence, constant rebuilt from verified codepoints with SECTION_SIMPLE embedded programmatically, reload round-trip asserted); low_data flag on AskRequest + ChatSendRequest (default off); tutor ask()/ask_stream() inject the short-answer instruction into extra_instruction; mock provider short-circuits to the simple section only when the instruction is present; test_low_data.py 3 green (low-data answer strictly shorter AND smaller json payload; keeps SECTION_SIMPLE drops SECTION_POINTS; default-off keeps SECTION_POINTS). Frontend: lib/offlineStore.ts IndexedDB 'bgpt-offline'/store 'chapters' with in-memory fallback so jsdom tests the same contract (save/load/list/delete + fetchChapterWithOfflineFallback network-first, saved-copy-on-failure, rethrow when nothing saved); LearnChapterPage queryFn uses the fallback with an offline chip (badge offlineBadge) and a per-chapter Download button (normalized offlineClass key shared by download/saved-probe/fallback); lib/lowData.ts persists bgpt-lowdata in localStorage, sets <html data-lowdata='1'>, initLowData() before first paint in main.tsx; MePage settings row adds a role=switch toggle; CSS append-only .switch + [data-lowdata] img/skeleton rules (hyphen byte-checked, tokens --m-fast/--ease/--radius/--line/--brand reused, none redefined); low_data sent from AITutorPage stream body and chapter Ask tab body. i18n bn+en keys downloadChapter/downloaded/offlineBadge/lowDataTitle/lowDataHint (codepoint script with anchor assertions; older 1.11/1.12 keys audited clean). Found + fixed a transport-induced identifier corruption introduced by an edit (fetchChapterWithOfflineStore -> ...Fallback), caught only by numeric count checks. vitest offlineStore.test.ts 6 + lowData.test.ts 5 green (suite 52/52); gates: pytest 231+3skip, ruff, mypy 49 files, tsc 0, vite build green. MANUAL NOTE for final report: real offline read (DevTools network disabled, saved chapter opens with the offline chip) will be exercised in the live-test pass — jsdom cannot disable the network. | commit: — (no-commit directive)
- [x] **1.14 PWA polish** | status: done ✅ | evidence: Install prompt lib/installPrompt.ts (deferred beforeinstallprompt captured w/ preventDefault, promptInstall() -> accepted|dismissed|unavailable, standalone detection via appinstalled + matchMedia display-mode, wasInstalled flag); AppShell header install icon-btn (aria-label i18n installApp, shown only when canInstall); main.tsx boot calls initInstallPrompt(). iOS/Android shell metas in index.html (mobile-web-app-capable, apple-mobile-web-app-capable/-status-bar-style/-title). Splash screen: rasterized icon.svg to pixel-verified icon-512.png + icon-192.png via headless Edge screenshot (cairosvg/PIL absent on this host); manifest icons[] extended (SVG any + maskable SVG + 192/512 PNG); sw.js CACHE bumped bgpt-v1->v2 and shell precache extended with both PNGs. vite.config preview.proxy /api -> 127.0.0.1:8000 (rewrite strip /api) makes the :4174 prod preview fully login-capable (verified /api/health 200). GATE G1 smoke ALL GREEN: health/ready/register/login/learn/tutor-grounded/quiz start (403-retry path exercised)/submit/explain/me/export/delete. ROOT CAUSE of the earlier persistent 'tutor grounded' failure: Windows curl corrupts non-ASCII bytes passed in -d argv (proof: od showed smoke.sh question bytes on disk clean e0a695..; raw UTF-8 sent via --data-binary @file grounded; identical -d argv refused). Fixed the HARNESS only: payloads now written by heredoc to $TMPD files and sent via --data-binary @file; question/explain assertion bytes kept byte-identical (R12-safe; no thresholds or tests touched). Lighthouse PASS-WHEN met: lighthouse@11 (Edge as Chrome; LH13 removed PWA category) on the prod build at :4174 -> installable-manifest, splash-screen (first run FAIL 'no PNG >=512px' -> fixed by the rasterized icons), themed-omnibox, content-width, viewport, maskable-icon = 6/6 automated PASS; pwa-cross-browser/page-transitions/each-page-url are manual-informational (n/a). vitest installPrompt.test.ts 5 green (cancelable-event fixture fix was a fidelity fix to the jsdom Event, not a weakened assert; suite 57/57); gates: tsc 0, vite build green, manifest parsed + brand token #4353b8 codepoint-checked, pytest/ruff/mypy unchanged (backend untouched this step). NOTE for live-test pass: streak visible, revision card, Me dark toggle, and true offline read (network disabled, saved chapter + offline chip) will be verified in the user-mode browser run. | commit: — (no-commit directive)
- [x] **2.1 School/class data model** | status: done ✅ | evidence: models: School (unique code), ClassRoom (school_id, class_level, section 'GEN' default, unique school+level+section), ClassStudent (unique classroom+student AND one-class-per-student v1 rule), ClassTeacher (unique classroom+teacher+subject, '' = all subjects); alembic f7a8b9c0d1e2 chained after e6f7a8b9c0d1 (append-only, R5 ok) with backfill: default school 'Default School'/'BGPT-DEFAULT' only when students exist, one GEN classroom per distinct student class_level, every student enrolled into its level's room (teachers NOT auto-assigned -- no per-class teacher data exists; stage 2 UI assigns explicitly); test_school_model.py 4 green (backfill counts exact 2x lvl6 + 1x lvl7; downgrade -1 removes all 4 tables leaving students intact + clean re-upgrade; empty DB => tables but zero rows; unique constraints for school code + classroom triple fire); dev.db was a create_all-era DB with empty alembic_version -- stamped f7a8b9c0d1e2 and ran the migration's exact backfill SQL by hand (10 students -> 1 school, 1 classroom, 10 links); gates: pytest 235+3skip (+4, R6 ok), ruff clean src+tests, mypy 49 files; teacher endpoints green (test_auth_teacher + test_pipeline 17 passed). | commit: — (no-commit directive)
- [x] **2.2 Class management UI** | status: done ✅ | evidence: backend -- StudentInvite model + migration b9c0d1e2f3a4 (SHA-256 code_hash only, 30d expiry, used_at; code doubles as initial password via must_change_password -- no new claim endpoint, smallest change R10); endpoints GET/POST /teacher/classrooms + GET .../roster + POST .../import (csv.reader, header skip, 422 empty/over-200-rows, per-row invalid/duplicate/created, duplicate emails case-insensitive, invite code secrets.token_urlsafe(9) returned once in response only); R11: imported students get consent_version='CSV-IMPORT-1' consent_at=now; test_classroom_import.py 5 green incl. PASS-WHEN 40-row CSV -> 40 invite-linked accounts (roster 40, code signs in, must_change_password true). frontend -- TeacherDashboard rewritten: class chips (.chip/.chip.active) replace the number input, new-class form (level 1-10 + section), roster per classroom with email + invite-pending badge, CSV import card (summary spans + per-row invite codes), assign/analytics cards keyed off selected room; 14 new i18n keys x2 dicts (11 S2.2 + 3 subject-* replacing hardcoded-Bengali options), codepoint-built + count-verified; teacherClassrooms.test.tsx 3 green (chip select/roster switch, create POST {class_level, section}, import POST + codes; test-mock fixes only, assertions real); gates: vitest 60/60 (+3, R6 ok), tsc 0, vite build green, file bytes hex-checked (0xb7 x2, 0x2014 x1 only). LIVE on restarted :8000 (now running 2.1/2.2 code): register/login teacher, classrooms list shows backfilled 6/GEN (10 students), POST 9/live -> 9/LIVE, import 4 rows -> created 3 + invalid 1 with distinct codes, roster invite_pending true, invite code signs in as student w/ must_change_password true, duplicate classroom -> 409. pytest 240+3skip. NOTE: display layer mutates 'flex'->'grid' in tool output; styles.css verified byte-clean via python token counts. | commit: — (no-commit directive)
- [x] **2.3 Content Engine** | status: done ✅ | evidence: services/generators/chapter_content.py -- generate_chapter_content(): retrieval (BM25, top_k 6, subject+level scoped) -> ONE provider.generate call -> strict JSON contract CONTENT_JSON_V1 (7 keys: summary, notes, key_points, examples, practice_qs, homework, exam_tips; parse_content_payload validates types/non-empty, ProviderError on garbage); injection-safe: evidence reused via tutor's EVIDENCE tags + sanitize_evidence, own system prompt states quoted-data rule and never-reveal rule; MockLLMProvider taught the contract (lazy import, deterministic JSON from evidence sentences). chapter_contents table (append-only versions, unique subject+level+chapter+version, payload JSON, source ai|teacher) via alembic c1d2e3f4a5b6 (chained b9c0d1e2f3a4, reversible; dev.db upgraded). endpoints: POST /teacher/content/generate (503 no provider, 502 provider/evidence failure -- NOTHING persisted on failure, IntegrityError retry for concurrent same-chapter), GET /teacher/content (current=max version), PUT /teacher/content (teacher edit appends new version), GET /teacher/content/history. naming collision found+resolved: services/learn.py already exported ChapterContentOut -> renamed our schema TeacherContentOut (existing learn API untouched). test_content_engine.py 7 green (single-AI-call asserted via counting fake provider; prompt carries CONTENT_JSON_V1+<evidence>; edit persists + no second AI call; history [2,1]; bad output 502 + 404 after; unknown chapter -> 0 AI calls; mock contract unit+integration; student 403s). gates: pytest 247+3skip (+7, R6 ok), ruff clean, mypy 51 files clean (fixed pre-existing _room_counts mypy error with repo-standard CursorResult cast). LIVE :8000 (mock): generate -> v1 ai + 4 sources, PUT edit -> v2 teacher, history + current verified via python urllib (curl 404 was AGAIN Git-Bash argv %-encoding corruption -- server correct). | commit: — (no-commit directive)
- [x] **2.4 Question Paper Generator (HIL)** | status: done | evidence: services/generators/question_paper.py -- plan_counts proportional easy/medium/hard (>=1 each), build_qp_prompt embeds SPEC line + evidence; ONE provider.generate call; THREE validation gates in validate_paper: NCTB alignment (per-question stem-term overlap vs evidence union >= 0.30), duplicate check (pairwise Jaccard <= 0.85), difficulty mix (each bucket within +/-18pp of target) -> ProviderError -> 502 with NOTHING persisted. question_papers table (questions JSON, status draft|final, per-question reviewed flags, meta audit: alignment_min/per-question + difficulty_actual vs planned) via alembic e5f6a7b8c9d0 (chained c1d2e3f4a5b6, dev.db upgraded). endpoints: POST /teacher/qpapers (qp_draft json_log elapsed_ms), GET list/one (ownership -- other teachers 404), regenerate/shuffle (review flags reset), review (accept/edit per question, edits persist to text/options/answer_index; reviewed_at set once ALL reviewed), replace (one-question swap from a fresh draft, normalized to original difficulty/chapter, 502 if no distinct text), FINALIZE 409 {code: review_required, unreviewed:[refs]} until every question reviewed -- explicit teacher action only; GET pdf ?kind=paper|answer. PDF route (R8: no faking Pango): WeasyPrint cannot load on this Windows host (needs GTK) -> primary engine fpdf2+uharfbuzz with bundled merged Noto Sans Bengali TTF (data/fonts, merged from @fontsource woff2 subsets, conjunct shaping verified via pypdfium2 PNG render); HTML print-view fallback; answer kind shows ANSWER KEY + correct-option marker. Generator bug found by tests: plan_counts called with wrong kwargs (**easy/medium/hard vs *_pct) -- fixed to positional. test_qpaper.py 14 green: draft<60s asserted from qp_draft log; difficulty split must sum 100 (422); garbage + alignment + duplicate + difficulty gate -> 502 nothing persisted; finalize-without-review 409 -> review all -> 200 final -> re-finalize/edit 409; edit persistence; shuffle reorder + reset; replace via varying provider keeps ref/label; ownership 404; PDF magic + Bengali extractable + answer-key marker via pypdf. web: TeacherDashboard question-paper card (spec form with difficulty mix, per-question accept/replace/edit-in-place, accept-all/shuffle/regenerate, finalize gated until all reviewed, authorized blob download of paper + answer PDFs); 16 i18n keys in BOTH dicts (Bengali byte-verified codepoints); vitest qpapers.test.tsx 4 green (64 total). gates: pytest 261 + 3 skip (+14, R6 ok), ruff clean, mypy 53 files, tsc clean, vite build green. LIVE :8000 (mock provider, dev.db): draft 201 in 0.04s (5 Qs, alignment_min 0.964 logged), finalize-before-review 409 review_required, accept+edit review -> reviewed_at set, shuffle reset, finalize 200 final, both PDFs %PDF magic with Bengali extractable; preview :4174 rebuilt + serves TeacherDashboard chunk containing the qp code. | commit: — (no-commit directive)
- [x] **2.5 Short Test ultra-fast path** | status: done ✅ | evidence: rule-based path chosen deliberately -- ClozeQuizGenerator (no LLM) generates ONE class+chapter question set per assignment, so generate->assign is far under the 10s p95 target; wall time logged as shorttest_assigned.elapsed_ms (json_log). ShortTest model (classroom+teacher indexed, questions JSON, attempts JSON [{student_id,attempt_id}]) via alembic f6a7b8c9d0e1 (chained e5f6a7b8c9d0, append-only, full downgrade; dev.db upgraded to f6a7b8c9d0e1 head). Per-student grading reuses existing quiz_attempts rows + /quizzes/{id}/submit flow -- no schema change there (R10). endpoints: POST /teacher/shorttests (room 404 / empty roster 422 empty_roster / no content 422 no_quiz_for_filter; one shared question set, attempt per student in a single transaction), GET /teacher/shorttests (own-scoped, admin all, classroom_id filter), GET /shorttests/mine (student: questions + own attempt_id + expires_at + advisory expired flag -- submitting after the soft window still grades, documented limitation: enforcement is advisory, no server-side submit block). test_shorttest.py 7 green incl. PASS-WHEN: 5-student assign logged elapsed_ms < 10000 AND wall < 10s; chapter filter (কোষ) exact-chapter corpus; expired=True after -3h storage rollback + post-expiry submit still 200; owner-scoped list; non-student mine []. web: TeacherDashboard short-test card (chapter/subject/count/duration -> one click assigns to whole room, recent list); QuizPage gets a shorttest tab (badge count, start opens the pre-created attempt through the existing quiz runner + result screen); 6+1 i18n keys x2 dicts (bn codepoint-fixed: টেস্ট uses U+099F not U+098F -- caught by codepoint audit); vitest shorttest.test.tsx 1 green (assign payload assert, disabled-until-chapter, stAssigned count, list render; 65 total). Existing qpapers/teacherClassrooms tests untouched except two real disambiguations my card introduced (label scope + null-safe list) -- assertions unchanged (R12 ok). gates: pytest 268 + 3 skip (+7, R6 ok), ruff check clean, ruff format --check now GREEN repo-wide at root (26 pre-existing unformatted files formatted -- AST-preserving), mypy 53 files, tsc 0, vite build green. LIVE :8000 (mock, dev.db): teacher qp.live -> room 1 (13 rostered after 3 CSV imports) -> assign 5Q/10min -> 201 with attempts=13 wall=51ms (p95 target 10000ms, ~200x margin); teacher list ok; student invite login -> change-password -> /shorttests/mine shows 5 questions attempt_id expired=false -> submit graded (total 5); unknown room 404; bogus chapter 422 no_quiz_for_filter; preview :4174 serves new bundles with /teacher/shorttests in TeacherDashboard-yOVevYpx.js and /shorttests/mine in QuizPage-DEIFZbx8.js. | commit: — (no-commit directive)
- [x] **2.6 Lesson Plan Copilot** | status: done ✅ | evidence: STATELESS design (R10): no table, no migration -- spec asks editable+printable, teacher edits/prints in the browser. services/generators/lesson_plan.py mirrors the 2.3 trio: LESSON_JSON_V1 contract, 8 keys in spec order (objective, previous_knowledge, introduction, main_explanation, activity, questions, assessment, homework), one index.search + exactly one provider.generate (no-hits -> ProviderError, no AI call); parse strips code fences, accepts only all-8-keys-complete payloads; any missing or blank value -> ProviderError. MockLLMProvider taught the contract (_lesson_json branch, minutes/level parsed back from the Target line). POST /teacher/lesson-plans (TeacherOrAdminUser; provider-less 503; ProviderError 502; json_log lesson_plan with elapsed_ms + plan_level -- renamed field, json_log() owns 'level'). schemas LessonPlanIn (minutes 5-180 default 35, level Literal beginner/average/advanced) / LessonPlanOut (sections dict + sources + echo). test_lesson_plan.py 6 green incl PASS-WHEN 'structured output test': single-AI-call fake provider asserts calls==1, prompt carries LESSON_JSON_V1 + <evidence> + 'minutes=35, level=advanced', response sections keys == exact 8-key order non-empty, sources cited; garbage 502; unknown chapter 502 with calls==0; student 403 / anon 401; mock contract e2e; parse unit (fenced ok, missing key/whitespace/no-json raise). web: TeacherDashboard lesson-plan card -- chapter/subject/minutes/level form (class_level from selected room), 8 labeled textareas (Bengali section labels, key map LESSON_SECTION_LABELS), edits are local-only (zero extra AI calls), Print button + append-only @media print block in styles.css (visibility gate: only .lesson-print prints, .lp-no-print hidden, textareas borderless 12pt -- design tokens untouched R5). i18n lp* x2 dicts (bn codepoint-audited; new lpChapterLabel keeps getByLabelText unique vs the S2.4 chapter input -- qpapers tests untouched R12). vitest lessonplan.test.tsx 2 green (POST payload + 8 labels in order + edit-without-refetch + print button; print-CSS source check via node:fs + ambient decl since vitest stubs css imports). gates: pytest 274 + 3 skip (+6, R6 ok), ruff check+format clean, mypy 54 files, tsc 0, vitest 67 (+2), vite build green. LIVE :8000 restarted on S2.6 code (dev.db): teacher qp.live POST 200 -- 8 sections exact order, all non-empty, 4 sources, echo minutes=40 level=advanced; student role 403; preview :4174 serves new build (lesson-plans found in TeacherDashboard-CXBIxonX.js). | commit: — (no-commit directive)
- [x] **2.7 Weak heatmap + at-risk + support plan** | status: done ✅ | evidence: Pure rules module services/atrisk.py (no DB needed for PASS-WHEN 'rule unit tests'): AT_RISK_AVG=40.0 strict, TREND_DROP=10pp halves-compare over >=4 chronological attempts else flat, is_at_risk = avg<40 OR trend down (None avg + flat = not flagged); build_plan -> deterministic weeks 1/2/3 concept->practice->assessment over the 3 weakest concepts, ValueError on empty. SupportPlan model + alembic a7b8c9d0e1f2 (append-only, dev.db upgraded to head). Endpoints (main.py, before /admin/users): GET /teacher/weak-matrix?class_level -- concept x student grid from AnswerLog joins (asked/correct/accuracy per chapter) + QuizAttempt graded score_pct list (avg/trend) + ChapterProgress read flag (the tutor-data signal); concepts sorted weakest-first by class accuracy; one bug fixed during verification: a leaked comprehension variable made every student share the last student's cells bucket -> per-student cells.get(s.id). POST /teacher/support-plans {student_id} (404 unknown; 422 'no graded quiz data' when nothing scored; weakest-3 by (accuracy,name); 201 + json_log support_plan_created). GET /teacher/support-plans (non-admin scoped to own teacher_id; student_id filter; newest first). test_atrisk.py 10 green (rule unit: avg, trend halves + <10pp flat, strict 40.0 boundary, None handling, week order + 3-cap; endpoints: matrix ordering ['beta','alpha'], per-student cells, read flag, plan create + other-teacher sees [], 404/422/student 403). web: TeacherDashboard heatmap card -- fetch rides room switch in load() (auxiliary: weak-matrix failure degrades to null, plans list to []); token-bucket cells (wm-bad <40 danger-soft, wm-mid <60 warn-soft, wm-good ok-soft, wm-na muted -- R5: existing tokens only, append-only CSS); avg/trend footer row (avg% + arrow), read cells carry title=t(wmRead); at-risk column badge badge-warn + per-student Create Support Plan buttons (at-risk only); plan list renders weeks 1-3 with i18n stage labels + focus detail. i18n wm*/sp* x2 dicts, bn codepoint-audited. vitest weakmatrix.test.tsx 3 green (cell class buckets + read titles + trend marks; at-risk badge count 1 + POST {student_id:12} + week lines matched via scoped function matcher; empty-state hint). gates: pytest 284 + 3 skip (+10, R6 ok), ruff check+format clean, mypy 55 files, tsc 0, vitest 70 (+3), vite build green. LIVE :8000 restarted on S2.7 code (dev.db, user-mode flow): teacher created room (6,WM), CSV import 2 students, invite password set, 2 short tests assigned, guaranteed-wrong submits -> weak-matrix 200 (concepts weak-first, cross-room aggregation incl. older Audit Student avg 33.33 flagged), 4 at-risk by avg<40 rule; POST support-plans 201 focus <=3 + weeks [1 concept, 2 practice, 3 assessment]; list 1 row; student 403 x2, anon 401. preview :4174 serves TeacherDashboard-Bm-WFA7T.js (weak-matrix + support-plans strings verified in the chunk). | commit: — (no-commit directive)
- [x] **2.8 Bulk assign + tracking** | status: done ✅ | evidence: Multi-student select -> one shared quiz + due date + completion list. Assignment model (teacher_id indexed, subject/chapter/num_questions, due_at, questions JSON shared set, attempts JSON [{student_id,attempt_id}]) via alembic b8c9d0e1f2a3 (chained a7b8c9d0e1f2, append-only, full downgrade; dev.db upgraded, single head). Endpoints (main.py, before /admin/users): POST /teacher/assignments -- student_ids 1-200 deduped via dict.fromkeys, 404 unknown_student, 422 mixed_class_levels (all selected students must share class_level), 422 empty list, 422 no_quiz_for_filter when the corpus is empty, 503 index_unavailable; one ClozeQuizGenerator set generated once (seed=random) then dump_quiz reused per QuizAttempt in one transaction, so 52 students cost ONE generation; due_at aware->naive-UTC normalisation for storage; json_log assignment_assigned with assignment_id/teacher_id/students/question_count/elapsed_ms. GET /teacher/assignments (owner-scoped, admin all, newest first). GET /teacher/assignments/{id}/progress -- PASS-WHEN completion list: one row per assigned student {student_id,name,attempt_id,done,score_pct,overdue}; done = attempt status graded; overdue = not done AND now > due_at; non-owner teacher 404. GET /assignments/mine -- student sees only their own rows with own attempt_id + overdue/done flags. test_bulkassign.py 5 green (PASS-WHEN 52-student assign with caplog assignment_assigned observed + per-student attempts; completion list correct incl. score_pct match after a real submit; overdue flag only for unfinished when due_at is in the past; validation + owner scoping + duplicate-id collapse; role guards student 403 x3, teacher mine [], anon 401). web: TeacherDashboard roster gets per-row + select-all checkboxes (aria-label baSelect: name), bulk card (chapter/subject/count/datetime-local due, Assign disabled until selection+chapter+due) + recent list with lazy Progress toggle -> completion table (badge-ok Done / badge-warn Overdue / badge Pending, score %). QuizPage gets a 4th tab আার নিয়োগ (asTitle) with count badge, per-card subject · due locale string, done -> badge tone ok / overdue -> tone warn, Start opens the pre-created attempt through the existing quiz runner (done rows disabled). i18n ba*/asTitle x2 dicts, bn codepoint-audited (the earlier failed insert used a partial anchor; rebuilt with the full bn value and re-audited). vitest bulkassign.test.tsx 2 green (tick 2 students -> payload student_ids/subject/chapter/num_questions/due_at asserted, selections reset after assign, empty-state clears; progress toggle fetches once, cached on reopen, shows Done 80% + Pending). gates: pytest 289 + 3 skip (+5, R6 ok), ruff check+format clean, mypy 55 files, tsc 0, vitest 72 (+2), vite build green. LIVE :8000 restarted on S2.8 code (dev.db, user-mode): teacher qp.live -> room (6,WM) roster [18,19] -> POST 201 assignment 1 with 5 questions + 2 attempts; progress before = both pending/not overdue; student wmkid0 /assignments/mine shows the row -> submit grades (score 0.0 all-blank); progress after = WM Kid Zero done 0.0, WM Kid One pending; mine done flag true; student POST/progress 403, anon POST 401. preview :4174 serves index-DAQb-KGk.js + TeacherDashboard-DAlAv-o7.js (assignments strings verified, chunk HTTP 200). | commit: — (no-commit directive)
- [x] **2.9 Question Bank** | status: done ✅ | evidence: services/question_bank.py: normalize_text (NFKC + casefold + whitespace collapse -- folds Bengali compatibility variants and Latin case), dedupe_key = sha256 of the normalized text, is_duplicate, store_reviewed (insert-or-return-existing, flush-only), find_reusable (class_level + optional subject/chapter, newest first, excludes texts already on the paper, limit param), bank_keys. QuestionBankEntry model (teacher_id, dedupe_key UNIQUE indexed, question_text, options/answer_index JSON, subject/chapter/class_level, source default qp_review, times_reused counter) via alembic c9d0e1f2a3b4 (chained b8c9d0e1f2a3, append-only, full downgrade; dev.db upgraded, single head c9d0e1f2a3b4). Wiring: (1) EVERY reviewed QP question enters the bank inside the existing review transaction (accepts AND edited text -- edited wording is what the teacher approved); json_log question_bank_reviewed bank_added/bank_total. (2) REUSE in generation: /teacher/qpapers/{id}/replace now tries the bank FIRST (MCQ-complete rows only: 4 options + int answer_index 0..3; marks inherited from the replaced slot so the paper total never drifts; invalid rows skipped, loop limit 8) and falls back to the unchanged AI draft path; qp_replace log gained reuse_source=bank|ai, bank row times_reused bumped. (3) REUSE METRIC LOGGED: qp_draft now logs bank_size/bank_matches/reuse_pct (drafted questions whose dedupe key is already banked). test_question_bank.py 5 green -- PASS-WHEN duplicate detection unit tests (pure normalize/key equality across whitespace + case + NFKC fullwidth variant, inequality on different content; service-level store across two teachers collapses to one row) + bank dedupe via endpoints (review 5 -> bank_added 5; re-review -> 0; sqlite row count stable) + reuse metric (deterministic mock re-draft: bank_size 5, bank_matches 5, reuse_pct 100) + bank-first replace (edited custom text lands on the new paper, reviewed=False, marks preserved, qp_replace reuse_source=bank, times_reused 1 via sqlite). gates: pytest 294 + 3 skip (+5, R6 ok), ruff check + format clean (4 files auto-formatted, no logic change), mypy 56 files. GATE G2 (full Stage-2 flow, live :8000 on 2.9 code, dev.db): class create (6,G2) -> CSV import 2 students (invite codes returned once) -> QP draft 5 questions -> 2 accepted keeps reviewed_at null -> remaining 3 accepted sets reviewed_at -> finalize 200 status final -> PDF export 11726 bytes (%PDF magic ok) -> short test assigned to whole room (2 attempts) -> both students invite-login + change-password + weak submits (score 0.0) -> weak-matrix 200 with 4 concepts x 18 students incl. both new students (heatmap populated, green) -> question_bank 5 rows / 5 distinct dedupe keys. | commit: — (no-commit directive)
- [x] **3.1 School onboarding** | status: done ✅ | evidence: Role migration: models.py User.school_id FK->schools.id (indexed, nullable) + SchoolInvite (code_hash UNIQUE, school_id, role teacher|school_admin, created_by/used_by/used_at) via alembic d0e1f2a3b4c5 (chained c9d0e1f2a3b4, append-only, full downgrade; SQLite path uses batch_alter_table because plain create_foreign_key raises NotImplementedError on ALTER-add; dev.db upgraded, single head d0e1f2a3b4c5). Schemas.py: RoleUpdateRequest Literal now includes school_admin; SchoolCreateIn/SchoolOut/SchoolInviteIn/SchoolInviteOut/SchoolJoinIn/SchoolStaffOut/SchoolOverviewOut. main.py endpoints: POST/GET /admin/schools (admin; 8-char code from confusable-free alphabet ABCDEFGHJKLMNPQRSTUVWXYZ23456789, collision re-roll); POST /schools/{id}/invites (admin|school_admin, single-use 10-char code, only _hash_invite SHA-256 stored, plaintext returned once, never logged); POST /auth/join-school (public; 404 invalid_code / 409 code_used / 409 email_taken incl IntegrityError race guard; creates User with the invite's role + school_id and a Teacher profile row, marks invite used, returns TokenResponse like /auth/verify-email); GET /schools/mine (SchoolStaffUser school_admin|admin; owns-school overview: students/teachers/classrooms + staff with class_teachers per-room counts; school-less admin falls back to default school); POST /schools/{id}/classes (room registered into that school). Scoping helper _school_or_403: admin any school, school_admin ONLY user.school_id else 403 code=other_school. teacher_create_classroom now lands rooms in the teacher's own school (legacy NULL-school teachers keep default school -> no S2.2 regression). test_school_onboarding.py 6 green PASS-WHEN (role migration green: school_admin appears, invite role round-trips to the joined User, PATCH /admin/users/{id}/role accepts school_admin 200; authz: teacher invite+join -> role/school_id in sqlite, /schools/mine 403 for plain teacher, school_admin principal can invite + register classes in OWN school (sqlite school_id), 403 other_school + 404 unknown school for the other school, anonymous 401, teacher invite create 403; code format [A-HJ-NP-Z2-9]{8}, plaintext code never in stored hash; single-use (2nd redemption 409 code_used), invalid 404, taken email 409; classrooms follow teacher school + legacy default school BGPT-DEFAULT). gates: pytest 300 + 3 skip (+6, R6 ok), ruff check + format clean, mypy 56 files. LIVE check :8000 on 3.1 code (dev.db, scratch rows cleaned after): register scratch root -> promote admin -> 2 schools created + listed -> teacher invite/join (role/school_id in DB) -> reuse 409 / invalid 404 -> school_admin principal own-school 201 + other-school 403 other_school + /schools/mine aggregates (2 staff) -> school-attached teacher /teacher/classrooms room lands in school A -> existing live accounts still login. Deferred to 3.2: /schools/mine is the data source for SchoolDashboard; Web UI has no school_admin route yet (role enum extended on API side only this step). | commit: — (no-commit directive)
- [x] **3.2 School Dashboard** | status: done ✅ | evidence: API GET /school/overview (school_admin|admin; SchoolHealthOut): whole-school students/teachers/classrooms counts, sessions_7d = conversations started in last 7 days (count ONLY -- zero message content, R11), Learning Health buckets strong(avg>=70)/support(40-70)/risk(at-risk rule from S2.7 atrisk: avg<40 OR downward trend)/ungraded, pct of graded, cross-class at-risk list (name, room class_level+section, avg, trend; worst first) reusing _attempt_percents + atrisk. Scoping: school_admin forced to own school (403 other_school on ?school_id), admin any school, plain teacher 403, anon 401. Schemas: SchoolHealthOut + SchoolAtRiskRow. Web: pages/SchoolDashboard.tsx (stat tiles, health card, at-risk table) + route /school RequireAuth role=school_admin + ROLE_HOME school_admin -> /school; i18n 10 keys x2 blocks inserted via codepoint script (byte-verified Bengali); types SchoolHealth/SchoolAtRiskRow. tests: test_school_dashboard.py 2 green -- PASS-WHEN aggregates match DB (6 students across 2 rooms, seeded graded attempts -> exact bucket counts + 40/20/40 pcts, sessions window excludes 30-day-old, at-risk rows carry right room/class_level) + authz scoping (teacher 403, anon 401, other-school principal 403 other_school + own-school empty overview 200, admin explicit school_id 200); vitest schooldashboard.test.tsx 2 green (renders buckets/at-risk rows incl. cross-class labels + empty state). gates: pytest 302 + 3 skip (+2, R6 ok), ruff check+format clean, mypy 56 files, vitest 74/74, tsc 0, vite build green (SchoolDashboard chunk in dist, preview :4174 serves new index-BiRMqzDi.js). LIVE :8000 on 3.2 code (dev.db, scratch rows cleaned): school+principal+teacher via invites -> 2 rooms + 6 CSV students -> raw graded attempts (strong/support/risk mix) + 2 recent + 1 old conversation -> /school/overview buckets 2/1/2 and 40/20/40 exact, cross-class at-risk [Kid3(GEN 6), Kid5(7)] -> teacher 403 / anon 401. Fix along the way (dev.db only, no code/migration change): dev.db had drifted from its stamped head -- conversations.last_strategy (migration a2c5e7b9d1f3) missing; full models-vs-dev.db column audit showed this was the ONLY drift; added the nullable column directly so dev.db matches head d0e1f2a3b4c5 again. | commit: — (no-commit directive)
- [x] **3.3 Curriculum coverage** | status: done ✅ | evidence: services/coverage.py pure derivation (PASS-WHEN unit tests): derive_status mastered(practiced + graded mean >= 70, threshold param)/practiced(any attempt incl. ungraded or low avg)/taught/uncovered with precedence mastered>practiced>taught; is_taught = chapter content for (class_level, subject) OR ClassTeacher assignment ('' = all subjects); avg_of. Endpoint GET /teacher/curriculum-coverage (teacher|admin): rows = classrooms in teacher's school (legacy -> BGPT-DEFAULT; admin all schools), subjects per room = content union student attempts union assignments; cell carries taught/attempts/attempts_graded/avg + status. Schemas CoverageCell/CoverageOut. Web: TeacherDashboard coverage card (class rows x subject columns, Badge tones ok/teal/default, cross-room empty dots), i18n 6 keys x2 blocks (codepoint-inserted, byte-verified Bengali; covTitle fixed to পাঠ্যক্রম আচ্ছাদন after a dropped-codepoint typo). tests: test_coverage.py 6 green (threshold boundary 69.9/70, ungraded -> practiced, low avg stays practiced not taught, assignment sources incl. '' marker) + test_curriculum_coverage.py 2 green (grid statuses mastered 85 / practiced 40 / ungraded-practiced / taught-by-content / taught-by-assignment, class-7 content does not leak into class-6 cells, no bangla cell without any signal; school scoping: other school teacher sees nothing of school 1, admin sees all, anon 401, parent 403). gates: pytest 310 + 3 skip (+8, R6 ok), ruff check+format clean, mypy 57 files, tsc 0, vitest 76/74 (+2) -- full-suite jsdom tests were brushing the 5s default timeout under parallel load, raised testTimeout to 15000 in vite.config.ts (timing room only, zero assertion changes), vite build green (TeacherDashboard-YoiCoYLF.js, preview :4174 serves index-BLe2MAVZ.js). LIVE :8000 on 3.3 code (dev.db, scratch cleaned): school+teacher invites -> rooms 6+7, 2 CSV students -> content/science+math@6, science@7 + ClassTeacher bangla@7 + attempts (80+90 science mastered, 40 math practiced, open english ungraded-practiced) -> grid exactly as derived, no bangla cell for class 6, anon 401. | commit: — (no-commit directive)
- [x] **3.4 Parent weekly digest v1.5** | status: done ✅ | evidence: services/parent_digest.py: summary-only weekly digest (R11: aggregates only, never message content or surveillance data; PASS-WHEN test seeds chat message + conversation title with a SENTINEL string and asserts it appears in neither body nor subject). digest_due(now,last_key) pure scheduler rule: Sunday 16:00 UTC (22:00 Dhaka), ISO-week dedup (real keys 2026-W36 -> 2026-W37 tested); week window excludes older attempts (9-day-old graded attempt not counted). collect_digest: sessions count, in-window graded attempts + avg via atrisk helpers, weak chapters = AnswerLog accuracy < 60 weakest-first max 3, at-risk flag -> 'needs support' / 'on track' phrasing; body renders per-child aggregate lines + bilingual footer 'summary statistics only'. run_weekly_digest: smtp_not_configured skip before any DB query when no sender injected; per-parent deliver via mailer.send_mail. Scheduler: parent_digest_loop asyncio task on startup/shutdown events (starts only under app lifespan; plain TestClient stays hermetic), checks every settings.parent_digest_check_minutes, ISO-week dedup in-process, errors logged not fatal. Settings parent_digest_enabled/check_minutes documented in .env.example (PARENT_DIGEST_ENABLED/CHECK_MINUTES). Bengali strings \u-escaped (transport-safe), codepoint-verified. tests: test_parent_digest.py 5 green (due-rule incl. dedup; build_body aggregates-only; live-shape seeded family -> exact line 'Rahim (class 6): 2 study session(s), 1 graded attempt(s), average 60.0% -- on track.' + 'Chapters to practise: Algebra' + no SENTINEL + parent with no linked students excluded; smtp skip; startup/shutdown task lifecycle). gates: pytest 315 + 3 skip (+5, R6 ok), ruff check+format clean, mypy 58 files. LIVE :8000 on 3.4 code: scheduler_started logged on boot, digest fired immediately (Sunday > 16:00 UTC) with skipped=smtp_not_configured; data-level dev.db check with capturing sender -> families=1 delivered=1, body carries aggregates + weak chapter, SENTINEL absent, all scratch rows cleaned. | commit: — (no-commit directive)
- [x] **3.5 Admin Center v1.5** | status: done ✅ | evidence: Admin Center v1.5 = school list + per-school stats + content versions + invite management, all admin-scoped. GET /admin/schools/stats (AdminSchoolStatsOut[]): per school teachers (role in teacher|school_admin) + classrooms + distinct enrolled students + sessions_7d (Conversations in last 7d, aggregates only, R11); reuses school/overview counting idiom. GET /admin/content/versions (ContentVersionRowOut[]): folds chapter_contents into one row per (subject, class_level, chapter) chain -> current_version=max, versions_total=len, source + updated_at + updated_by_email from the max-version row. Invite management: GET /admin/schools/{id}/invites (SchoolInviteAdminOut: id/school/role/used/created_at/used_at, NEVER code or code_hash R7) + DELETE /admin/schools/{id}/invites/{iid} (204 revoke; 404 unknown/cross-school; 409 already_used so redeemed invites stay for audit; revoked code can no longer redeem). All four behind AdminUser (anon 401, non-admin 403). Web AdminDashboard admin-center card (rides main load, aux catch): create-school form, schools table (name/code/teachers/classes/students/sessions_7d + per-row role select + Invite button showing plaintext once), click name -> invite list (role/status Active|Used/created + Revoke on unused), separate Content versions card (subject / class / v{cur} ({total}) / source / updater email). i18n 22 keys x2 blocks, Bengali inserted via escaped-codepoint scripts (admInvites fixed U+09F0 -> U+09B0) all char-range verified. tests: test_admin_center.py 3 green (stats counts with 8-day-old session excluded; invite list/used+active, no code leaked, revoke 204 -> revoked code 404s on redeem, used-revoke 409, unknown 404, teacher 403, anon 401; versions ordering + chain v3(3) teacher + null updater). vitest admincenter.test.tsx 3 green (stats + version rows, empty hints, invite-once + revoke flow hitting del). gates: pytest 318 + 3 skip (was 315+3: +3 new admin tests, zero regressions, R6 ok), ruff check+format clean, mypy 58 files, tsc 0, vitest 79/79 (+3), vite build green (index-BsmMCdrm.js, TeacherDashboard-BdcUkVwu.js). LIVE :8000 on 3.5 code (dev.db): admin login -> stats BGPT-DEFAULT teachers=0 classrooms=4 students=20 sessions_7d=0; content versions shows real teacher-edited Bengali chapter 'কোষ' v2 total2 source=teacher + updater email; invite create id=1 -> list shows active, no code field -> DELETE 204 -> list empty (scratch cleaned, revoked invite removed); anon /admin/schools/stats 401. | commit: — (no-commit directive)
- [~] **G3 gate — code-local part** | status: local PASS, staging deploy PENDING (human input, never faked) | evidence: dev.db seeded via :8000 (script kept at apps/api/_g3_local.py, idempotent): admin -> "G3 Model School" + school_admin invite (g3head@g3.test) + teacher invite (g3teach@g3.test, pw G3Live2026x); teacher -> 2 rooms (6 GEN + 7 GREEN) + 2x40 CSV students; seeded graded attempts -> school_admin GET /school/overview shows EXACTLY students=80 teachers=2 classrooms=2 strong=30 support=20 risk=16 ungraded=14 (12 low-avg <40 + 4 down-trend [80,80,40,40]), at_risk list 16 rows weakest-first, cross-school ?school_id -> 403. Side-fix during setup: swept orphan teachers/students/quiz_attempts rows in dev.db (pre-S2 rowid-reuse leftovers; not app code, dev-db hygiene only). | commit: — (no-commit directive)
- [x] **4.1 Education Context Engine** | status: done ✅ | evidence: services/context.py = the single context home: frozen RequestContext{role,class_level,subject,chapter_id,goal,history_summary,mastery_snapshot} + render_block() (trusted [education-context] ASCII-header block, prepended to the USER prompt ahead of untrusted evidence -- all 4 system prompts byte-identical, R5) + log_fields() (counts/ids only, R11) + history_summary() (turn count + strategy, never text) + snapshot_mastery() (weakest-first, cap 8, None-skip, 1dp) + ContextVar propagation mirroring request_id_var. Central builder main._request_context(): per-user Student mastery from AnswerLog per-chapter accuracy, logs event ai_request_context on EVERY AI call. Wired all 5 provider paths / 9 build points: tutor ask + ask_stream (context param), /tutor/ask (goal question|quiz_explain), chat message + stream (goal chat|reteach, history N turns + last_strategy), /teacher/content/generate (chapter_content), _qp_fresh_draft x3 call sites (question_paper, question_paper_replace), /teacher/lesson-plans (lesson_plan). gemini provider logs now carry context fields via get_current_context(). tests: test_request_context.py 10 green (unit: render determinism, log fields counts-only + JSON-safe, history summary, mastery snapshot cap/round/None; wiring: prompt starts with block + SYSTEM_PROMPT byte-equal on all paths, R11 sentinel never in any log line, mastery reaches prompt from seeded attempt history (25% weakest chapter), reteach goal + '2 turns, strategy=' from real second turn, SSE stream carries context, all three generators context-driven with byte-equal system prompts). gates: pytest 328 + 3 skip (was 318+3: +10 S4.1 tests, zero regressions, R6 ok), ruff check+format clean, mypy 59 files, tsc 0, vitest 79/79, vite build green (unchanged web hashes). LIVE :8000 on 4.1 code (dev.db, scratch student s41live, self-deleted 204, 0 orphans swept): grounded ask 200 (3 sources) + reteach chat 200, server log shows ai_request_context lines {ai_role:student ai_goal:question|reteach ai_history:'0 turns, strategy=simple' ai_mastery_concepts:0} -- counts only, no content (R11). | commit: — (no-commit directive)
- [x] **4.2 AI Model Router** | status: done | evidence: services/router.py = rules-first deterministic classifier (never an LLM call, zero added latency): TOOL_GOALS (chapter_content/lesson_plan/question_paper/question_paper_replace) -> TOOL; COMPLEX triggers: >160 chars, >2 question marks (ASCII + Bengali visarga), analytical markers (4 Bengali codepoint constants + compare/explain-why/analyze/derive/prove-that/summarize-the-chapter) or why-and-how multi-hop construction, >2 sentences (danda-aware split); else SIMPLE. Route ContextVar propagates the route per request (same pattern as request_id_var). fast_eligible: SIMPLE only AND only when a fast lane is really configured. Fast lane: config gemini_fast_model + providers.get_fast_provider (None unless gemini active with a distinct model); create_app builds it under ProviderNotConfigured guard, registers aclose on shutdown, injects TutorService.fast_provider; .env.example documents GEMINI_FAST_MODEL (empty -> main serves all routes, routes still decided+logged, R8: real staging fast-model choice is a human input). Generators hard-pin TOOL via set_current_route(Route.TOOL) before provider.generate (main model + RAG). Per-request logging: ai_call event route+model+latency_ms+prompt_chars/answer_chars (cost proxy, no content, R11) on ask and ask_stream; gemini provider lines embed ai_route via _context_log_fields(). PASS-WHEN replay: replayed sample traffic built from the 36 real chapter titles of the sample corpus + analytical long tail + teacher tool calls -> route_distribution/simple_share = 36 simple / 4 complex / 3 tool = 83.7% simple, asserted >=70% in test. tests: test_model_router.py 9 green (rules: tool goals, short facts simple, all complex signals incl. Bengali markers imported from source constants, fast-lane eligibility; wiring: SIMPLE question hits fast recorder only + ai_call route=simple model=fast with all 5 fields, COMPLEX stays on main, fast-absent keeps main on SIMPLE, teacher generation hits main on TOOL; replay >=70%). gates: pytest 337 + 3 skip (was 328+3: +9 S4.2 tests, zero regressions, R6 ok), ruff check+format clean, mypy 60 files (router.py added) clean, web untouched (tsc 0 / vitest 79/79 / build hashes unchanged). LIVE :8000 on 4.2 code (mock provider, dev.db, scratch student s42live, self-deleted 204, 0 orphans): grounded simple ask 200 -> log route=simple model=mock (no fast lane in mock mode -> main serves SIMPLE, correct), 3-question ask 200 -> route=complex; every ai_call carries latency_ms + prompt_chars + answer_chars; ai_request_context lines remain counts-only. | commit: — (no-commit directive)
- [x] **4.3 RAG v2** | status: done | evidence: section chunking (v1 TextIngester, chunk.meta.section) -> retrieval/embedding.py Embedder protocol + HashingEmbedder (char n-grams 2-4 blake2b -> 384-dim L2-normalised counts, deterministic, dependency-free; letters-only compaction so punctuation cannot share grams; build_embedder factory: EMBEDDING_MODEL set -> ProviderNotConfigured, R8 real multilingual model + pgvector = staging human input) -> retrieval/vector.py VectorIndex (cosine over precomputed vectors of vector_text = chapter > section + text, heading context in the vector; BM25-compatible filters) -> retrieval/fusion.py reciprocal_rank_fusion (score = SUM 1/(k+rank), k=60, ties first-seen) + retrieval/hybrid_index.py HybridIndex (BM25 lane + vector lane each candidate_k=3*top_k -> RRF -> lexical_rerank: final = BM25-lane base + 1.0*rrf + 0.5*stem-coverage + 0.3*trigram; the lexical base keeps hybrid scores on the BM25 scale so /search min_score=0.5 and generator grounding keep their semantics, R12) -> top_k. Grounding guard: hashed-bucket collisions give a gibberish query a fake positive cosine ("xyzzy-not-in-corpus" shares ASCII co/in with cos/sin in trig formulas), so vector-only candidates must share >=1 Bangla bigram with the query (embedding.bengali_bigrams) or they are dropped; gibberish now returns [] exactly like the BM25 baseline. Wiring: config retrieval_mode default "hybrid" (RETRIEVAL_MODE=hybrid + # EMBEDDING_MODEL= in both env examples); main._build_index + RankingIndex protocol (retrieval/base.py) into tutor and the 3 generators (type-only change). PASS-WHEN eval: golden hit@5 set of 25 verified (query, class, subject, expected) pairs = 16 standard recall + 9 short phonetic/loanword lookups (verified hybrid wins where BM25 returns 0 or misses top-5: কোশ কী->কোষ, সূচক কী->সূচক, পাইথাগোরাস উপপদ্য->পিথাগোরাস, হাইপা কী->অতিভুজ, ফ্লোয়াম কি->ফ্লোয়েম); measured hit@5 BM25 20/25 -> hybrid 25/25 = +25.0% relative lift, asserted >= +20% with a full-recall regression guard. tests: test_rag_v2.py 11 green (RRF math, embedder determinism/normalisation/separation, R8 factory guard, vector_text headings, VectorIndex filters, HybridIndex drop-in contract, rerank rewards lexical overlap, create_app wiring hybrid-default + bm25 opt-out, golden lift) + 2 pre-existing 502-grounding tests restored by fixing code not tests (R12). gates: pytest 348 + 3 skip (was 337+3: +11 S4.3 tests, zero regressions, R6 ok), ruff check+format clean, mypy 65 source files clean, web untouched. LIVE :8000 on 4.3 code (mock, dev.db, hybrid default): grounded simple ask 200 grounded:true 3 sources -> ai_call route=simple; 3-question ask 200 grounded:true -> route=complex; every ai_call keeps model/latency_ms/prompt_chars/answer_chars; ai_request_context stays counts-only (R11); /search hits + ask_action ok for standard queries; unknown-chapter generation still 502 without AI call; 0 dev.db orphans. | commit: — (no-commit directive)
- [x] **4.4 Knowledge Graph v1** | status: done | evidence: models Concept/ConceptPrerequisite/ConceptMastery (concepts unique name+class+subject; source chapter|llm; aliases JSON) + alembic a4f7c2e8b1d0 chained from d0e1f2a3b4c5 -- dev.db round-trip verified (upgrade -> downgrade -1 tables gone -> upgrade recreates with indexes; dev.db initially got the tables from startup create_all, so they were dropped and re-created via the real migration for schema truth). data/kg/prerequisites.json: 17 curated edges + 2 aliases, every name byte-verified against the loader's 37 chapter roots (generator script asserted against corpus; bash heredocs corrupt Bengali literals, so all Bengali data went through the Write tool). services/knowledge.py: pure layer (build_concept_seeds chapter-root dedupe; build_edge_pairs pins same-name chapters to the LOWEST class and skips unknown names; prereq_closure BFS min-depth; resolve_gaps canonicalizes mastery keys merging aliases, weak = total>=3 AND pct<50, surfaces never-practiced or weak transitive prerequisites nearest-first) + LLM layer (extract_chapter_concepts -> parse_concept_names accepts a name ONLY if it occurs verbatim in the chapter text and differs from the chapter title -- R12 grounding guard; mock provider therefore yields roots-only, honest) + DB layer (ensure_concepts idempotent seeding, rebuild_concepts full re-seed + optional llm pass, record_quiz_result credits name-in-question llm concepts + always the chapter root, mastery_map merges same-name roots, student_gaps). Bugs found+fixed during verification: pending ConceptMastery rows read correct=None (column defaults apply only at INSERT) -> explicit zeros; resolve_gaps ignored alias-keyed mastery -> canonicalize+merge so the spec's literal sentence works. Wiring: quiz-submit mastery hook (after revision, index-guarded), GET /kg/gaps (student; teacher -> 404 no_student_profile, no fabricated empty), POST /kg/rebuild (admin-only, ?llm=, closure provider). schemas KgGapOut/KgGapsOut/KgRebuildOut. PASS-WHEN: tests/test_kg.py 11 green -- the spec gap test literally: mastery {"বীজগণিত": (1,5)} surfaces "ভগ্নাংশ" at depth 1, prereq_pct None (concept reported under canonical "বীজগণিতীয় রাশি" via alias); literal-guard test pins every Bengali constant against the live corpus + shipped edge data; weak/strong/min-attempts/transitive-depth rules; seed ordering + lowest-class pinning; verbatim parse guard; deterministic DB-layer session test (ভগ্নাংশ class-6+7 roots merge, accuracy stays 20% -> gap surfaces); API shape + auth guards. gates: pytest 359 + 3 skip (was 348+3: +11 S4.4 tests, zero regressions, R6 ok), ruff check+format clean, mypy 66 files. LIVE :8000 on 4.4 code (mock, dev.db, self-registered class-7 student): admin POST /kg/rebuild -> {concepts:37, edges:21, llm_concepts:0}; 3 quizzes all-wrong (20%) -> GET /kg/gaps -> exactly [{concept: ভগ্নাংশ, prereq: স্বাভাবিক সংখ্যা, depth 1, never practiced}] codepoint-verified; student rebuild 403; anon 401. | commit: — (no-commit directive)
- [x] **4.5 Adaptive practice engine** | status: done | evidence: services/adaptive.py Elo layer - DEFAULT_ELO 1200 / K 24 / SIGMA 200 (spec: next Q = ability + 0.5 sigma -> next_target; selection = argmin |item elo - (chapter ability + 100)|, ties by id so deterministic), pure expected_score/update_pair zero-sum (correct -> ability up AND item elo down), item identity = generator fingerprint sha256(chunk|term)[:10] (stable across attempts, so per-item difficulty accumulates); 2 tables practice_items/student_abilities via appended migration c7d1e4f6a2b8 (alembic heads ok, dev.db upgrade -> downgrade -1 -> upgrade round-trip clean); wiring: quiz start = 3x pool (cap 30) then adaptive.pick_questions; submit = adaptive.apply_review (sequential Elo per answer). Regression found+fixed: db.get cannot see PENDING rows, so one review covering the same chapter twice inserted duplicate student_abilities PKs (500 on live sessions with autoflush=False) - explicit fresh-row cache + pinned regression test. Wrong answer -> KG gap check (S4.4 student_gaps) -> re-teach card = verbatim excerpt from the PREREQ chapter's own chunk (sentence-bounded, <=280 chars, cap 3, zero AI calls); QuizStarted/QuizResult gained default-empty reteach fields (API-compatible); next quiz start carries open_reteach_cards. WEB: QuizPage re-teach cards on the result screen and before Q1 of the next quiz; i18n bn+en (reteachTitle/reteachHint/reteachChain, codepoint-verified); styles.css appended token-only .reteach-*; preview dist rebuilt. STAGE FIX (code, not tests - R12): adaptive selection changed quiz content and the generic 'explain this' phrasing diluted the tutor coverage gate (combined query coverage 0.36-0.40 < min_coverage 0.5 -> test_quiz_explain end-to-end refused with insufficient_evidence; measured with a live gate probe); fix = search_query override on tutor.ask/ask_stream so retrieval+gate run on the quiz item itself (0.56-0.60) while the student's own phrasing still feeds the evidence prompt and routing; SYSTEM_PROMPT untouched (R5 byte-parity). PASS-WHEN (spec: adaptation unit tests correct->harder, wrong->easier+teach): test_adaptive.py 10 green - difficulty ladder: 10 correct answers must pick a strictly HARDER item, 10 wrong a strictly EASIER one, gap-check card excerpt verified verbatim in prereq-chapter text, duplicate-chapter regression, determinism-under-ties, 2 API round-trips. Gates: pytest 369 + 3 skipped (was 359 + 3: +10, zero regressions R6 ok), ruff check+format clean, mypy 67 source files clean, tsc --noEmit clean, vitest 82 (was 79: +3 re-teach UI tests). LIVE :8000 (mock, dev.db): fresh student 3x5 all-index-0 math quiz -> StudentAbility 1158.1/1126.0 below the 1200 baseline, 9 PracticeItems elo 1187.2-1234.1 (both directions), submit cards ভগ্নাংশ->স্বাভাবিক সংখ্যা (d1), শতকরা->ভগ্নাংশ (d1), শতকরা->স্বাভাবিক সংখ্যা (d2, transitive prereq closure), round-1 of a barely-practiced student honestly empty (min 3 attempts), submit cards subset of next-start cards, and post-fix explain smoke on the live server grounded:true with 3 sources. | commit: — (no-commit directive)
- [x] **4.6 Weakness rollup** | status: done | evidence: services/weakness.py = the single source of truth for per-concept weakness (spec: nightly job -> per-concept mastery -> feeds Home + Teacher at-risk + Parent digest). Input = lifetime graded AnswerLog cells canonicalized via knowledge.canonical (alias counts merge); rule = the exact S4.4 constants total >= knowledge.MIN_ATTEMPTS (3) AND pct < knowledge.WEAK_THRESHOLD_PCT (50), so KG gap resolution and every consumer agree by construction; order = (accuracy asc, name) so 'weakest first' is identical everywhere. Helpers raw_cells/merged_cells/_rank + weak_concepts/weak_names + weak_names_for (batched: one query per class roster, no N+1 in the matrix). nightly_due(now,last_day) pure day-key scheduler rule mirroring digest_due (fires 21:00 UTC = 03:00 Dhaka, once per calendar day); refresh_mastery(db) = the nightly reconciliation: recomputes chapter-root ConceptMastery rows straight from the graded answer log and creates any missing chapter-root concept, idempotent, returns counts only (R11: students_synced/rows_synced logged, nothing identifying). Consumers wired to the ONE function: /dashboard/summary (student progress + weak_quiz recommendation), the /students/{id}/progress builder, the parent /parents/me/children/{id}/progress builder, teacher GET /teacher/weak-matrix -> new WeakStudent.weak_concepts (schema, default []), and parent_digest.collect_digest now slices the rollup (its private weekly <60 rule + WEAK_CHAPTER_ACCURACY constant deleted). Scheduler: weakness_refresh_loop asyncio task + startup/shutdown events beside parent_digest_loop; settings weakness_refresh_enabled/check_minutes documented in .env.example (WEAKNESS_REFRESH_ENABLED/CHECK_MINUTES); server logs weakness_refresh_scheduler_started / weakness_refresh_run{students,rows}. WEB: WeakStudent.weak_concepts optional type, TeacherDashboard weak-matrix headers render the weakest-3 rollup as badges per student (pure concept names, no i18n change needed). Deliberate non-consumers (documented): ClassAnalytics.weak_chapters stays the pooled class-difficulty cohort stat and teacher support-plans still rank any graded chapter ['weakest 3 by accuracy'] -- neither is a per-student mastery verdict. tests: test_weakness_rollup.py 5 green: nightly_due rule + merged_cells alias merge (uses a real curated pair, no hardcoded Bengali) + rollup thresholds (0% and 25% weak; exactly 50% NOT weak; 0% with 2 attempts NOT weak) + THE PASS-WHEN: one seeded student -> home progress weak_chapters == teacher weak_concepts == parent progress weak_chapters == ['alpha','beta'] AND the digest email body carries 'Chapters to practise: alpha, beta' (run_weekly_digest with injected sender; home recommendation chapter == weakest) + refresh_mastery exact mastery_map merge + idempotency. Updated per spec (single-source rule, assertions tightened not weakened -- R12): test_quiz_progress weak assertion now encodes accuracy < 50 AND asked >= 3 (was < 60). gates: pytest 374 + 3 skipped (was 369 + 3: +5, zero regressions, R6 ok; one interim red caught by the env gate -- .env.example must list every Settings field -- fixed by documenting WEAKNESS_REFRESH_*), ruff check+format clean, mypy 68 source files clean (weakness.py added), tsc 0, vitest 83 (was 82: +1 rollup-chips UI test; bulkassign/lessonplan flakes reproduced only under parallel pytest load, 83/83 idle-machine), vite build green. LIVE :8000 on 4.6 code (dev.db, scratch s46<stamp> student/teacher/parent): nightly refresh_mastery synced 12 students / 28 rows across the whole dev.db and mastery_map == the seeded cells exactly; HOME weak_chapters ['alpha','beta'] + recommendation weak_quiz chapter=alpha; TEACHER weak_concepts ['alpha','beta'] with at_risk=True; PARENT progress ['alpha','beta']; CONSISTENT True across all three over real HTTP; weakness_refresh_scheduler_started logged on boot. | commit: — (no-commit directive)
- [x] **4.7 Evaluation v2 (CI regression gate)** | status: done | evidence: golden set v2 = eval/golden_v2.json 634 items (624 positives + 10 negatives; 78/78 corpus sections; all 11 (class,subject) pairs; min pair 25) generated deterministically by scripts/gen_golden_v2.py under a documented CONSTRUCTIVE rule (R12: expected values come from construction, never from observed results): 'lift-and-ask' questions built from each section's OWN vocabulary (gloss '<term> ki?' + consecutive 2/3-term content windows asked as '<window> ki?'), kept only when (a) the section text satisfies the tutor's own grounding gate (replicated TutorService._gate: len>=2 terms, light_stem, coverage >= 0.5) and (b) the same class+subject-filtered hybrid index returns the section's own chapter top-1 with score > 0 (top_k=3 mirrors runtime); negatives' key tokens are ASSERTED absent from the whole corpus at generation time. First construction (4 diluting templates, weak own-chapter top-1 rule) produced expected answers the honest gate refused (grounded 47.7%) -- fixed by TIGHTENING the construction rule to the gate, NOT by editing expectations. Metrics (evaluation/metrics.py): grounded-rate, coverage_true/false + refusal precision, faithfulness (LLM-judge when a provider is configured; otherwise an honest deterministic evidence-overlap mode recorded as local_overlap -- no run can fake an LLM score; R8), grammar heuristic (Bengali-letter sentences, no Latin, danda/? closure; mock scaffolding [mock]/bullets/section-labels stripped as formatting not content; definition pinned by unit tests). Baseline eval/baseline_v2.json; compare_gate: any of the 6 metrics dropping MORE than GATE_PP=2.0 percentage points vs baseline FAILS (exactly 2.00pp passes per spec '>2%'). CLI python -m bangla_gpt_api.evaluation.cli is HERMETIC: forces llm_provider=mock even if the local .env sets gemini (a first unforced run hit real Gemini 429 -- proof the env leaked), local hashing embedder, no secrets/network; --write-baseline is a manual reviewed step; --seed-bad N injects N deliberately-wrong items (golden negatives flipped expect=answer, cycled so N can exceed 10 on a 600+ suite). Recorded baseline on current code (identical on every rerun -- deterministic): total 634, grounded_rate 0.9984, coverage_true 0.9984, coverage_false 1.0, precision_true 1.0, faithfulness_avg 0.8548 (honest: mock's unsupported check-question costs overlap), grammar_avg 0.9008. PASS-WHEN DEMO (recorded): eval green = GATE: PASS exit 0; --seed-bad 14 -> GATE: FAIL exit 1 with grounded_rate drop +2.15pp and coverage_true +2.19pp flagged FAIL(>2pp), every injected miss refused_reason=insufficient_evidence; rerun unseeded -> GATE: PASS exit 0 byte-identical metrics (revert = removing the CLI flag; golden/baseline files were never touched -- R12 clean). One golden failure KEPT honestly: a class-8 science question about 'youn projonon' (sexual reproduction, legitimate NCTB syllabus) is refused with refused_reason=sexual_content -- safety-filter false positive on age-appropriate curriculum text; left in the golden set as visible debt that drives S4.8 age-appropriateness (metrics improve honestly; golden never edited to go green). Workflow .github/workflows/eval-gate.yml added LOCALLY only (never pushed per directive): ubuntu py3.12 job running evaluation.cli against the committed baseline; deliberately never runs --write-baseline (baseline is a reviewed artifact; re-baselining-to-green is forbidden). tests/test_eval_v2.py 10 green: golden shape (>=300, all 11 pairs >=10 each, no duplicate questions, ASCII bytes), negative-token corpus-absence recheck, grammar-score units pinning the heuristic, local_overlap 1.0/0.5/0.0 units, llm-judge parse 0.75 + unparsable->0.0 never-silent, compare_gate boundary (exactly 2.0pp PASS / 2.01pp FAIL / improvements PASS / GATE_PP==2.0), the seeded-bad PASS-WHEN as an integration test on a hermetic 22-item subset (honest baseline 1.0 grounded -> seeded FAILS gate -> revert deterministic + green), baseline has the 6 compared metrics + grounded >= 0.95 (G4 pre-check), and the workflow-file-present test. gates: pytest 384 + 3 skipped (was 374 + 3: +10, zero regressions R6 ok), ruff check + format --check clean (147 files; also fixed a pre-existing I001 in _g3_local.py), mypy 70 source files clean; web untouched this step (vitest/tsc/build unchanged at 83/0/green). | commit: — (no-commit directive)
- [x] ~~**4.7 Evaluation v2**~~ | superseded stale duplicate row (this step was completed above; row never reconciled during the no-commit build-out)
- [x] **4.8 Safety v2** | status: done | evidence: (1) Prompt-injection filter on corpus ingest: services/safety.py strip_injections() drops EN override patterns ("ignore previous instructions"/"reveal your system prompt"/...) and Bengali leak sentences; applied inside TextIngester.flush() before chunking, counted in dropped_injections (R11: count only). Tests: every sample-corpus chunk survives byte-for-byte (dropped==0); a poisoned doc loses exactly the injected sentences (3) and stays byte-clean otherwise; academic reproduction text (spaced + no-space forms) is never treated as injection; sexual keyword outside academic context still blocks. (2) Age-appropriateness rules in SYSTEM_PROMPT: AGE_RULE_SENTENCE asserted byte-identical in ALL FOUR SYSTEM_PROMPTs (R5 protected constants otherwise untouched); safety-screen academic-term override grounds the class-8 science item 'youn projonon' recorded as debt in S4.7 -- golden grounded_rate improved 0.9984 -> 1.0 with golden/baseline files NEVER edited (R12: improvement allowed, expectations never). (3) Refusal audit view: GET /admin/safety/refusals (admin-only: anon 401, student 403; days clamped 1..365) returns COUNTS ONLY -- total_refusals, by_reason, by_class, last_refusal_at -- test asserts question text absent from the response body (R11); AdminDashboard safety card (stat row + by_reason table + per-class chips + last refusal) + 6 i18n keys x2 language blocks (codepoint-verified) + vitest card test (counts render, empty state). (4) Red-team set into eval: eval/redteam_v2.json, 16 items from scripts/gen_redteam_v2.py (deterministic, md5 f7a4d02f52cada17466ef679103117ba stable across regens) -- EN/BN prompt-leak, role override, poisoned-corpus-echo, jailbreak personas; evaluation.cli --redteam hermetic gate (Settings(env='test', llm_provider='mock') forced in main() even though local .env configures gemini): 16/16 refused, coverage_false==1.0, no failures, leak markers (SYSTEM_PROMPT[:40], "system prompt", "SYSTEM PROMPT", "<system>") absent from every answer -> "REDTEAM: PASS" exit 0. Spec PASS-WHEN met twice over: red-team eval green (CLI exit 0 + pytest integration) AND injected corpus text cannot leak the system prompt (chunk-level TextIngester proof + runtime ask proof). LIVE proof vs restarted :8000 (server itself runs REAL Gemini via hidden apps/api/.env): 10/10 -- academic override grounded=True, sexual_content refusal for non-academic use, self_harm refusal with support copy incl. Bengali-DIGIT hotline (ASCII digits would fail), BN leak attempt refused + zero leak markers, admin audit 200 counting our refusals with by_class, R11 body-content check, student 403, refusal turn 0.051s. BUG FOUND+FIXED via live stress (40 real streamed calls): Gemini 429 during SSE did NOT surface as ProviderError -- gemini.py _error_message_text() passed the parsed dict into _error_message(), which expects an httpx.Response, so dict.json() raised AttributeError, escaped main.py's `except ProviderError` and killed the SSE connection mid-turn (server log traceback). Fix: shared _error_detail() payload parser used by both helpers; regression test test_stream_error_after_retries_raises_provider_error (fails AttributeError-style without fix, passes with -- 10/10 in test_providers_gemini.py). Post-fix server handled 16 real 429s with ZERO tracebacks: retry-then-clean `event: error {"code":"llm_unavailable"}` + db.rollback (no orphan user turn). | GATE G4 recorded honestly: (a) grounded-rate >=95% on golden PASS -- hermetic golden_v2 634 items grounded_rate 1.0, coverage_true 1.0, coverage_false 1.0, precision_true 1.0, all 6 compared metrics >= baseline, GATE: PASS exit 0. (b) >=70% fast-route PASS -- classify() replay over all 634 golden questions: 631 SIMPLE = 99.5% (rules-first router: <=160 chars, <=2 questions, <=2 sentences, no tool markers). (c) AI TTFB p95 <=2s -- measured honestly, split in two: app-side plumbing (screen+retrieve+gate+SSE; refusals and error turns) p95 ~0.08s PASS; real-model first-token on THIS dev machine with the real key (gemini-3.1-flash-lite, free-tier quota, mid-run 429s + retries): n=35 grounded AI turns p50 2.2s / p95 4.0s / max 6.7s -> FAIL on dev, NOT faked into a pass. Context: GEMINI_FAST_MODEL unset so fast and main routes both serve the main model; choosing the fast model is R8 external-model human input, and the authoritative real-model TTFB venue is staging (G3 🖐) where fast-model routing/paid quota/region are set. G4 verdict: (a)(b) unconditional PASS, (c) app-side PASS + real-model measurement recorded pending the staging human step. | gates: pytest 400 + 3 skipped (was 384+3: +15 safety_v2 +1 provider regression, zero regressions R6 ok), full vitest 85 passed (+2 refusalaudit), tsc 0 errors, vite build green, ruff check + format --check clean, mypy 70 source files clean. Dev note: dev.db accumulated stress-run refusal counts (insufficient_evidence entries from a harness mistake: forcing all golden questions through one student class; retrieval is class-scoped by design -- NOT a product bug) -- dev-only, audit is counts-only (R11). | commit: — (no-commit directive)
- [x] ~~**4.8 Safety v2**~~ | superseded stale duplicate row (completed above)
- [x] **5.1 Environments + IaC** | status: done | evidence: staging now runs with production strictness -- config.py PRODUCTION_ENVS = frozenset({"production","prod","staging"}) drives is_production, so the boot guard (enforce_production_safety: refuses default/short JWT secret, weak admin creds) and secret-suppression behave identically on staging; Settings env values compared via strip().lower() so "staging " / "STAGING" cannot dodge the guard. New drift guard tests/test_env_parity.py 5 green: (1) .env.example keys are all real Settings fields; (2) .env.production.example keys subset Settings UNION a 12-entry INFRA_ONLY allowlist of compose-only vars (ACME_EMAIL, API_TAG, WEB_TAG, BACKUP_INTERVAL_SECONDS, BACKUP_KEEP_DAYS, DOMAIN, GRAFANA_ADMIN_PASSWORD, POSTGRES_DB, POSTGRES_PASSWORD, POSTGRES_USER, VITE_API_BASE, WEB_CONCURRENCY) -- any new deploy var must be documented or the test fails; (3) every ${VAR} reference in docker-compose.yml is documented in an example env or has a compose default; (4) development/test/staging all instantiate; (5) explicit staging-shares-production-strictness assertion (incl. trailing-space variant). docs/runbook.md section 2 updated: staging parity note + pointer that test_env_parity.py is the env-surface drift guard. gates: full pytest 405 passed + 3 skipped exit 0 (was 400+3: +5 S5.1 tests, zero regressions R6 ok); ruff check+format clean; mypy unaffected (config change is a frozenset constant). Honest scope: live fresh-clone staging bring-up needs a real staging host = human input (R8); bring-up procedure documented in runbook sections 1-3. | commit: — (no-commit directive)
- [x] **5.2 Zero-downtime deploy** | status: done | evidence: (a) mechanism verified multi-worker-safe: Dockerfile CMD gunicorn -k uvicorn_worker.UvicornWorker -w ${WEB_CONCURRENCY} --graceful-timeout 30, HEALTHCHECK /health, /ready gate endpoint (200 only after boot guards pass and the search index is built). (b) docs/runbook.md section 3: expand-contract migration convention (add nullable columns -> backfill -> dual-write -> contract only in a later release -> downgrade stays one release behind) + rolling switch procedure referencing the script. (c) NEW permanent scripts/deploy_rolling.py orchestrator mode: polls GET /ready on the NEW instance until 200 (timeout), and ONLY when green runs --reload-cmd (graceful caddy reload) then --drain-cmd (stop old); if the gate never goes green NOTHING is touched, exit 1, old release keeps serving. (d) --demo mode = self-contained local proof of the mechanism: two real app instances (mock provider, fresh temp sqlite with verified start-cleanup of stale db/-wal/-shm sidecars), old release gates green and serves through an in-process proxy whose upstream flips atomically, 3 concurrent load workers hammer /health across boot -> gate -> flip -> drain. Debug loop recorded honestly: earlier demo FAILs were a harness sequencing bug -- cold-booting both instances against one sqlite file concurrently raced; fixed by matching the real runbook sequence (old must already SERVE before the new one boots) + sidecar cleanup; the diagnostic instance on :60999 was stopped and its temp db deleted. Result: two consecutive runs `demo: requests=32 failures=0` -> ZERO-DOWNTIME SWITCH: PASS (0 failed requests), exit 0, artifacts self-clean; ruff check + format --check clean (no noqa: asyncio.to_thread(subprocess.run, check=False), narrow exception handling, contextlib.suppress). No app-code change (script is standalone), so 405+3 stands. Formal deploy-under-load on a staging host = human input (R8), procedure in runbook section 3. | commit: — (no-commit directive)
- [x] ~~**5.1 Environments + IaC**~~ | superseded stale duplicate row (completed above)
- [x] ~~**5.2 Zero-downtime deploy**~~ | superseded stale duplicate row (completed above)
- [x] **5.3 Redis** | status: done | evidence: NEW caching.py -- Cache protocol (get_json/set_json/clear) with two backends behind ONE infra switch (S5.1): RATE_LIMIT_BACKEND=redis + REDIS_URL -> RedisCache shared by all pods; otherwise bounded in-process MemoryCache (TTL + max-keys eviction + deep-copy in/out so callers cannot corrupt entries); identical API either way. Redis failure NEVER breaks a request: every Redis op degrades to a cache miss with a count-free warning (R11). (1) /dashboard/summary: per-user 60s cache (spec TTL), key summary:{user_id}:{date} (date-rollover safe); endpoint refactored into _build_dashboard_summary + cache wrapper -- payload schema unchanged for the frontend. (2) RAG query cache: CachedRankingIndex transparently decorates the BM25/Hybrid index at _build_index, caching full hit lists for 300s; query stored as SHA-256 only (test asserts the raw question string is absent from every cache key -- R11); class_level+subject are part of the hashed key so class-scope isolation cannot leak (S4.3 golden-set lesson honored); empty results cached too; Hit/Chunk JSON roundtrip fidelity asserted (text + meta + score). (3) Multi-instance consistency PROVEN LOCALLY against REAL Redis 7 (throwaway docker container, not faked -- R8 respected: real server, no staging secrets): 3 REDIS_URL-gated tests (skipif otherwise) -- RedisCache roundtrip+TTL; two RedisRateLimiter objects as two pods share one global fixed-window limit (10 allowed across BOTH, 11th denied on either); pod B's index reuses pod A's cached retrieval (inner.search never called). Plus fail-soft test (connection refused -> miss, no raise) always runs. Live server :8000 restarted with the redis backend: ALL LIVE S5.3 CHECKS PASS -- summary:* key present in redis and second call served cached in 10ms; /search kohash-key stable across calls, rag:* keys all hashed (no plaintext query), rl:* limiter keys flowing. PASS-WHEN completeness: cache-hit METRICS visible -- bgpt_cache_events_total{cache,result} Prometheus counter incremented on every summary/rag lookup, asserted present on the unauthenticated /metrics scrape in test_cache_hit_metrics_visible_on_metrics_endpoint. docs/runbook.md section 8: cache semantics + 60s dashboard staleness window documented. gates: full pytest 420 passed + 6 skipped exit 0 (was 405+3: +15 always-on cache tests +3 real-Redis-gated, zero regressions R6 ok; the same 18 cache tests also run 18/18 PASSED with REDIS_URL set), plus the metrics test landed after that run (targeted suite 19+3 green; full-suite re-run scheduled at the next gate -> 421+6); mypy 71 source files clean, ruff check + format clean on src/tests/scripts (151 files). Remaining human input: production Redis lives in the compose stack (already provisioned as a service) -- nothing to fake. | commit: — (no-commit directive)
- [x] ~~**5.3 Redis**~~ | superseded stale duplicate row (completed above; production Redis lives in the compose stack)
- [x] **5.4 Background jobs (ARQ)** | status: done | evidence: Idempotency moved from per-process dicts (the pre-5.4 bug: gunicorn -w N meant N digest emails per Sunday) into a DB ledger: NEW JobRun model + alembic migration e8f9a0b1c2d3 (job_runs, composite PK (job, period_key)); migration applied to dev.db AND cycle-verified (upgrade -> downgrade -1 -> upgrade, DDL inspected). NEW jobs.py: claim_period() = one INSERT whose IntegrityError loser skips the period -- covers restart-during-period, two gunicorn workers racing, web loop + ARQ cron at the same wall clock, retried queue delivery; run_weekly_digest()/run_nightly_rollup() wrap due-rule + claim + work + finish_period(outcome), shared by BOTH schedulers (single source of truth). main.py: both loops now call jobs.run_* via asyncio.to_thread (no event-loop blocking) and stay silent when JOBS_BACKEND=arq; create_app boot-rejects any other JOBS_BACKEND value. NEW worker.py: ARQ WorkerSettings, cron entries mirror the services' rules (Sunday>=16 UTC digest, 21 UTC rollup), engine/session built at worker startup not import. Wiring: arq added to pyproject deps, Dockerfile COPY worker.py, docker-compose worker service (profile worker, single-instance semantics documented), JOBS_BACKEND documented in both .env examples (compose interpolation defaulted -> parity guard green). LIVE PROOFS (real infra, R8 honest): (a) arq worker booted against REAL Redis 7.4 in docker -- log shows both job functions + both cron entries registered, redis_version line printed; (b) SMTP SEND PROOF: threaded real-protocol SMTP sink (greeting/EHLO/STARTTLS with generated cert/AUTH PLAIN/MAIL-RCPT-DATA) on 127.0.0.1:8025 -- app's untouched mailer.send_mail delivered BOTH transactional emails (verify-email + password-reset); tokens read from the RECEIVED messages and redeemed: /auth/verify-email 200, /auth/reset 200 issuing JWT, login with new password 200 -- "email actually sends AND the credential works end to end". TLS-trust patch lives only in the scratch harness (real relays present trusted certs); production relay credentials remain 🖐 human (no faking an external provider). Idempotency tests tests/test_jobs_v2.py (10): double-claim skip + fresh-period claim, THREE schedulers racing one Sunday -> exactly 1 email + 2 losers already_ran + next week fires, not-due calls never burn a ledger slot, nightly double-run single recompute, invalid JOBS_BACKEND boot RuntimeError, inline starts loops vs arq silences them, worker WorkerSettings shape (redis URL parsed, cron weekday=6/hour>=16, rollup hour=21). Honest scoping: PDF generation is on-request (exists), corpus ingest is a CLI (stage 6), revision scheduling is data-driven due_date queries -- no cron needed for those. gates: FULL pytest with REDIS_URL + live Redis 7.4: 434 passed + 3 skipped exit 0 (was 420+6 -> +14 executed including the 10 job tests, zero regressions R6; same run also validated the redis-lib 4.x->5.3.1 bump and the arq install); mypy 73 source files clean (redis 5.3 ships py.typed and exposed 2 union types in ratelimit/caching -> honest narrowings, no behavior change); ruff check+format clean; env parity tests green with the new worker service + JOBS_BACKEND. docs/runbook.md: new "Scheduled background jobs (inline vs ARQ)" section. Server :8000 restarted on S5.4 code (health/ready green, provider=gemini real). | commit: — (no-commit directive)
- [x] ~~**5.4 Background jobs (ARQ)**~~ | superseded stale duplicate row (completed above; production SMTP relay creds stay ✍)
- [x] **5.5 Scale audit + load test** | status: done ✅ (local; staging k6 gate 🖐) | evidence: full pagination/N+1 audit of main.py hot paths; fixed response-shape-preservingly: roster briefs 1+N -> chunked single GROUP BY (limit 1-1000), /tutor/conversations counts -> one GROUP BY, conversation messages capped (default 200, newest-N chronological), /assignments/mine scan window + batched attempt fetch (JSON-membership semantics documented; link-table deferred as proper fix), /teacher/assignments/{id}/progress 1+2N -> 2 batched IN queries, qpapers/shorttests/assignments lists limit+offset. Rationale-preserved items listed in report (admin content versions, school stats, CSV import, quiz-submit PK gets, AnswerLog scan cached). test_scale_v2.py 5 green incl. N+1 PROOF (before_cursor_execute capture: exactly 1 quiz_attempts statement for 4 students) + NULL-score/empty-conversation value parity. Deliverable scripts/load/k6_scale.js (constant-arrival 500 RPS read mix dashboard/search/revision/health + 50 RPS /learn/progress writes, thresholds p95<400 both mixes + failed<1%, setup() single login vs 10/min limiter, node --check clean); /events excluded after proof its 60/min/IP limiter 429s a single generator IP (measures limiter not API). Honest local numbers (k6 absent, SQLite, laptop): 1 worker ~41-48 RPS p95 91-412ms; 4 workers ~60-75 RPS p95 67-173ms through conc 32, p95 466ms at conc 64 (SQLite write contention); open-loop 550 RPS attempt FAILED loudly (~34 RPS, 78% client conn errors) -> 500/50 target NOT claimable locally, PASS gate = staging k6 run when creds provided. docs/scale_report.md saved (audit table, honest numbers, upgrade path). gates: full pytest with REDIS_URL: 439 passed + 3 skipped exit 0 (484s; 434+5 = exactly the new file, R6 ok), ruff src+tests clean, mypy 72 files clean; :8000 restarted on S5.5 code (single worker, redis ratelimit, real Gemini env intact). | commit: — (no-commit directive)
- [x] **5.6 Security hardening** | status: done ✅ | evidence: (1) AUDIT TRAIL -- new audit_log table (migration f9a0b1c2d3e4, upgrade->downgrade-base->upgrade cycled, alembic check no drift) + write_audit() in security.py; ALL FIVE event types wired+proven: role_change (PATCH /admin/users/{id}/role, detail {from,to}), data_export (GET /users/me/export, counts/fact only), purge (POST /admin/maintenance/purge, counts-only detail, test asserts), qp_finalize (teacher draft->review->finalize, target qp:{id}), impersonation (start row carries reason, stop row phase=stop). Admin view GET /admin/audit: admin-only (401/403 tested), action filter, limit 1..200 + offset bounds (422s tested), newest-first by id. (2) IMPERSONATION AUDITED -- POST/DELETE /admin/users/{id}/impersonate: admin-only, refuses admin/school_admin targets (403 impersonation_forbidden), 15-min token with {imp:true, imp_by} claims (pyjwt-decoded in test: ttl 14-15min, works as student, /admin/audit still 403); honest limitation recorded: stateless JWT = no early revoke before ceiling (A08 in checklist, revoke list = S5.10 follow-up). (3) PII ENCRYPTION -- parents.phone_enc Fernet (cryptography dep added; PII_ENC_KEY in .env.example + .env.production.example, names only R7; generated key now in gitignored apps/api/.env, value never logged); raw-sqlite proof: cell startswith "fernet:", digits absent from whole DB file; roundtrip via /users/me; export never leaks phone (tested); prod boot refuses without key (enforce_production_safety + dedicated test with positive control). (4) CSP -- API: per-response nonce in SecurityHeadersMiddleware (rotation proven in pytest AND live: two curls showed different nonces), /docs /redoc /openapi.json carve-out documented as dev-surface risk; SPA: stock caddy:2-alpine CANNOT mint nonces (proven on v2.11.4: {http.request.id} prints literal, no request_id directive) -- pivoted to strictly stronger no-inline-script: theme/language boot moved to public/theme-boot.js static file, Caddy now ships plain script-src 'self' on SPA (docker-verified: CSP header on / and on theme-boot.js, SPA deep routes 200), dist rebuilt (only file-src scripts). (5) CI SCANS -- api job pip-audit (clean) already present; web job gained npm audit --audit-level=high --omit=dev (0 prod vulns; deployed artifact is static nginx). Triage register docs/security_scans.md: vite <=6.4.2 dev-server advisories (.map traversal, launch-editor NTLMv2, fs.deny Windows) + esbuild <=0.24.2 dev CORS GHSA-67mh-4wv8-2f99 -- all dev-machine-only, fix = vite major bump deferred (R10). (6) docs/owasp_checklist.md -- A01-A10 honest code+test pointers, [PARTIAL]/[RISK] items listed (JWT HS256, /docs carve-out, no lockout beyond rate-limit, impersonation no-revoke). Tests: tests/test_security_v2.py 12 green; 2 old tests updated for the intended new contract (test_account_deletion /users/me exact-dict gains "phone": None; test_password_reset prod-guard fixture gains pii_enc_key + new PII_ENC_KEY refusal case) -- assertions strengthened not weakened (R12). Gates: FULL SUITE REDIS_URL: 451 passed + 3 skipped exit 0 (523s; 439+12 = exactly the new file, R6 ok), ruff src+tests clean, mypy 73 files clean; :8000 restarted on S5.6 code (exec_7688e227, redis ratelimit 6399, real Gemini .env intact) with live checks: /health ok, CSP nonce rotates per response, /admin/audit 401 unauth. | commit: — (no-commit directive)
- [x] **5.7 Backup / DR** | status: done ✅ (local drill PASS; weekly cadence on staging 🖐 staging-server-pending) | evidence: (1) pgBackRest LIVE WIRING -- deploy/postgres/Dockerfile (postgres:16 + pgbackrest 2.59.1 binary + stanza conf + nightly loop BAKED IN via COPY; Windows bind mounts of repo paths silently yield missing files, proven: container saw no such file for a mounted conf -- baked image instead); docker-compose postgres service now runs wal_level=replica + archive_mode=on + archive_command=pgbackrest --stanza=bgpt archive-push %p + archive_timeout=60, new pgbackrest sidecar (profile backup) nightly full loop, volumes pg_sock/pg_data/pgbackrest_repo shared, depends_on required:false keeps single-profile parse clean; compose validated via docker compose config all profile combos. (2) RESTORE DRILL (PASS-WHEN) -- scripts/pgbackrest_drill.sh (the weekly staging artifact, host-agnostic docker-only): throwaway cluster :5433, pre-marker, stanza-create+check+full backup, POST-backup marker + pg_switch_wal, volume wiped, restore, restart; verified 2026-09-07: markers "pre-backup,post-backup" recovered, postgres log shows restored log file 000000010000000000000005 FROM ARCHIVE + archive recovery complete + timeline 2 -> WAL replay PROVEN (not dump-reload); restore+startup wall 9s vs 14400s RTO budget. Drill fixes learned: pgbackrest refuses root (exec -u postgres + gosu), trust socket needs pg1-user=bgpt (superuser is bgpt not postgres), MSYS_NO_PATHCONV guard for /repo mangling. (3) RPO/RTO DOC -- docs/backup_dr.md: RPO ~60s (archive_timeout) vs 24h budget, RTO measured 9s vs 4h w/ runbook steps, retention 14F/7D zstd, off-site = repo1-type=s3 or rclone, key-loss caveat (PII_ENC_KEY deliberately NOT backed up), SQLite dev fallback = B12 backup_loop+restore_test. (4) S3-COMPATIBLE STORAGE -- scripts/s3_corpus_sync.py (lazy boto3, any S3 endpoint incl MinIO/R2, refuses keys escaping --dest) + QP PDFs documented as DERIVED (render_qp_pdf from DB rows -> DB restore restores PDFs, no separate target); S3_BUCKET/S3_PREFIX in .env.production.example + INFRA_ONLY parity allowlist. (5) NEW TESTS -- tests/test_backup_dr.py 4 green (stubbed-S3 sync incl path-escape refusal; compose+conf drift guard; drill self-containment). (6) drive-by fix: .env.production.example committed value ALLOW_DIRECT_PARENT_LINK=false1440 -> =false (pre-existing prod-boot parsing bug, byte-verified via git show HEAD). Gates: full pytest REDIS_URL 455 passed + 3 skipped exit 0 (828s; 451+4 = exactly the new file, R6 ok), ruff src+tests+script clean, mypy 73 files clean; :8000 untouched (no app-code change this step). | commit: — (no-commit directive)
- [x] **5.8 Compliance** | status: done ✅ (local; 🖐 Bangladesh legal review still human-pending) | evidence: (1) RETENTION JOB -- services/retention.py single policy point shared by admin endpoint AND nightly arq job; POST /admin/maintenance/purge?dry_run=true = counts-only report with ZERO deletes/commits and NO audit row (test proves all row counts unchanged after the report); real sweep deletes only expired rows (180d chats+messages, 30d-grace expired reset/verification tokens, USED expired invites -- unused ones keep working till their own expiry), fresh conversation+message provably survive, audit detail = 5 int counts only, dry_run key filtered out of detail (R11); nightly job jobs.run_retention_job claims once per calendar day (2nd call already_ran dict-exact, next-day runs, idempotent 0-counts); LIVE dry-run on :8000 2026-09-07 -> {"dry_run":true, all counts 0} (fresh prod-ish DB, honest zero). (2) CONSENT BUMP FLOW (PASS-WHEN green) -- CONSENT_VERSION 2026-08-v1 -> 2026-09-v2; GET /students/{id}/consent reports needs_reconfirm when stored consent_version != current (bump -> every older-accepted student must re-confirm); POST /students/{id}/consent/reconfirm refreshes version+at+ip evidence; accepted=false -> 400 consent_not_accepted; authorize_student_access enforced (owner ok; other student + unlinked parent 403; unauth 401 -- all tested). tests/test_compliance_v2.py 7/7. (3) DATA-MINIMIZATION review docs/data_minimization.md -- field-by-field KEEP/MINIMIZED/NONE table (consent_ip justified as legal evidence only; phone Fernet at rest; reset/verify/invite tokens hash-only + swept; /events persists NOTHING -- logs name + prop KEYS never values; audit counts-only; no provider transcript logs) + honest gaps (no dormancy purge yet, consent-evidence horizon is policy not code). (4) SCHOOL DPA docs/dpa_template.md -- controller/processor roles, children's data (classes 6-10, guardian-consent-gated), sub-processor register incl Google Gemini prompt-data scope, deletion SLA [14]d, breach notice [48/72]h, audit rights, end-of-term return/delete, Bangladesh governing law -- marked TEMPLATE pending counsel; 🖐 Bangladesh legal review = human step, NOT performed. Gates: FULL suite REDIS_URL 462 passed + 3 skipped exit 0 (565s; 455+7 = exactly the new file, R6 ok), ruff src+tests+worker.py clean, mypy 74 files clean (retention.py new); test_jobs_v2 worker-shape test WIDENED to the 3-function/3-cron shape incl hour==20 assertion (stricter, R12 ok); :8000 restarted on S5.8 code (exec_995bdad1, redis 6399, real Gemini .env intact) -- live checks /health ok, consent + purge 401 unauth, openapi lists /students/{id}/consent + /consent/reconfirm + /admin/maintenance/purge. | commit: — (no-commit directive)
- [x] **5.9 Distribution** | status: done ✅ (local artifacts; PASS-WHEN "APK builds / test push on device" = 🖐 Android keystore+Firebase+device, honestly NOT faked) | evidence: (1) PWA PROD BUILD VERIFIED -- npm run build (tsc+vite) green; dist/ contains manifest.webmanifest (standalone, theme #4353b8, Bengali name, 192/512+maskable icons), sw.js (app-shell cache-first, navigations network-first + offline fallback, API NEVER cached -- child-data rule), theme-boot.js, icons; SW registration PROD-gated (main.tsx); served on :4174 preview (exec_bba961ef) with correct content types (manifest=application/manifest+json, icons image/png, all 200). (2) CAPACITOR ANDROID WRAPPER -- @capacitor/core+cli+android 7.6.9 (prod-dep audit 0 vulns), capacitor.config.ts (appId app.banglagpt.tutor, webDir dist, allowMixedContent false -- TLS-only API), npx cap add android + cap sync PASS (web bundle copied into android/app/src/main/assets/public, generated capacitor.config.json keys verified via node), npm scripts cap:sync / cap:android; android/ project files git-visible (check-ignore clean; template .gitignore covers builds). Native build doc docs/mobile_release.md: VITE_API_BASE absolute-URL requirement (WebView origin can't use '/api'), CORS must add capacitor://localhost + https://localhost. (3) 🖐 NOT POSSIBLE HERE, recorded not faked: no JDK/java + no ANDROID_HOME on this machine (probe: where java empty, Program Files/Android absent) -> gradlew assembleDebug must run on a build machine (JDK21+SDK35 steps in doc); FCM push (spec: teacher assign + parent digest) needs Firebase project + google-services.json + push plugins + server sender -- API has NO push sender and NO code claims one; today those notifications go by email (S5.4 mailer); iOS needs macOS/Xcode (🖐). Gates: web vitest 85 passed (same count as before, R6), npm run build green twice (with capacitor.config.ts present), npm audit --omit=dev 0, backend untouched this step (462+3 stands). | commit: — (no-commit directive)
- [x] **5.10 Support ops** | status: done ✅ | evidence: audited impersonation now revocable (jti in token + POST /auth/impersonate/exit parks jti in shared cache, get_current_user rejects; 15-min expiry = backstop; per-session not per-user); feedback triage queue = alembic a1b2c3d4e5f6 (triaged/triaged_at/triage_note on existing feedback, cycled upgrade/downgrade/upgrade, alembic check no drift) + admin GET/PATCH /admin/feedback (oldest-first, open/all filter, open_count, reporter identity withheld per R11); public GET /status = presence/booleans only (db/cache.ping()/provider, no counts or config disclosure); web: AdminDashboard triage card + support-check launch (reason prompt), AppShell acting-as banner with server-side-exit button, public /status page + footer link; support-ops i18n strings kept English in both dicts (deliberate: prevents mis-voialed Bengali in support tooling; dedicated Bengali pass = follow-up). Gates: pytest 470 passed + 3 skipped exit 0 (8 new tests/test_support_ops.py incl. triage flow + revoke + per-session isolation), ruff src+tests+worker.py clean, mypy 74 clean; web vitest 89/89 (4 new supportops.test.tsx; admincenter/refusalaudit mocks extended for new queue call), tsc+build green. Live :8000: /status ok payload; impersonate 200 → token live 200 → exit 204 → same token 401, audit phases [start, exit]; full live triage round-trip (register 201, feedback 201, queue open 1 → triage+note stored → open 0, all 1), probe account deleted + no orphans in dev.db; :4174 serves rebuilt bundle (index-pvEKgOfH.js). | commit: — (no-commit directive)
- [x] **S5.10 final user-mode live test (browser, :4174)** | status: done | evidence: drove the app as a real user end-to-end: public /status via footer link renders All-systems-operational with db/cache/assistant rows; registered throwaway student (consent checkbox, guardian gate), auto-land on /student; AI tutor answered a Bengali question with grounded 3-section answer + textbook evidence chips + next-step chips (screenshot verified single render); helpful-vote created feedback row (id 1) visible in admin triage card with NO reporter email (R11); triage note stored + Mark-triaged moved row to Triaged/Open 0; support-check impersonation (reason prompt, prompt stubbed only because the in-app browser auto-dismisses window.prompt dialogs -- app logic untouched): banner 'acting as QA Live Student', admin token backed up, End-support-session returned to /admin with keys cleared; server side: audit phases [start, exit] with imp_by on user:133, jti present in redis imp_revoke:* scan. Two small UI fixes found by the live pass: status-page refresh aria-label was the page title (now refreshBtn key), triaged status cell showed the button label instead of a Triaged label (new i18n key admTriaged, both dicts; live re-check shows Triaged + Reopen). Post-fix gates: tsc 0, vite build green, vitest FULL 89/89; backend untouched (pytest 470+3 stands). QA account deleted via /users/me (204), feedback=0, orphans=0. | commit: — (no-commit directive)
- [x] **S6.2 Content QA (auto quality report + 10% human sampling flow)** | status: done | evidence: nctb/content_qa.py + scripts/content_qa.py CLI (report/sample/review; report exits 3 when flagged ratio > threshold); tests/test_content_qa.py 13 tests; full backend suite 483 passed + 3 skipped exit 0 (baseline 470+3, +13, R6 intact); ruff + mypy clean; PASS-WHEN proven: seeded bad chunk c0042 flagged (math_mojibake+zero_width), 10%-bad corpus blocked by gate, empty corpus never counts as pass; NFC/ZWNJ/math-mojibake/PUA/Bijoy checks | commit: — (no-commit directive)
- [x] **S6.5 Govt/authority anonymized aggregate export** | status: done | evidence: services/govt_report.py (k-anonymity suppression min_cell=5, fail-closed PII scanner, CSV formula-escape, CSV BOM for Excel+Bangla, PDF via fpdf2+Noto) + admin-only GET /admin/reports/aggregate?format=json|csv|pdf (district is an export label only, no schema change); tests/test_govt_report.py 9 tests green - seeded canary emails/names appear in NO format, zero '@' in exports, small cells suppressed; live :8000 verified 200 for json/csv/pdf, suppressed_cells=2, total_students_included=84 | commit: — (no-commit directive)
- [x] **S6.6 Capacity plan + webfont subsetting (measured)** | status: done | evidence: docs/capacity_plan.md (10k/100k/1M MAU sizing from measured baselines: 2 workers=120 concurrent journeys, entry JS 281KB->84KB gz, Bengali UI fonts ~175KB real cold payload; LLM ~$0.024/student/month flagged as assumption needing procurement check); scripts/font_subset_report.py measured: full Noto TTF->UI subset 71.8% reduction, shipped-font dir 317KB->244KB (-23.0%), latin-ext subsets 74-92% droppable; decision: keep @fontsource for pilot, revisit at 100k | commit: — (no-commit directive)
- [~] **S6.7 Hosting decision doc** | status: drafted, ✍ human sign-off PENDING (R8 - not faked) | evidence: docs/hosting_decision.md - 4 options scored on residency/child-data, reliability, cost, CI/CD fit, headroom; recommendation: single VPS Singapore (region = sign-off input) for pilot, managed Postgres + 2nd node at ~50k MAU, k8s only on measured triggers; unsigned sign-off block included - no provisioning or spend authorized | commit: — (no-commit directive)
- [x] **S6.8 Load + chaos game-day** | status: done (local; staging legs stay ✍ human) | evidence: docs/game_day_2026-09-08.md - capacity ladder 0% errors through 240-way concurrency (~2x pilot peak; 1 worker ~65 RPS plateau, p95 1.9s@120 / 3.5s@240, graceful queueing); limiter drill: burst abuse shed as clean 429s (~70% of ask traffic, health 100%); LLM-down (:8020 fake key): /tutor/ask -> structured 502 llm_unavailable (no leak), quizzes+auth+status stay 200; finding: /status assistant check is presence-only (stays ok during upstream outage) - accepted for pilot, option (b) cached probe flagged for human; drill ran on temp DB copies + separate ports, preview stack and real Gemini quota untouched | commit: — (no-commit directive)
- [x] **Phase H — v0.5.0 release finalization** | status: done ✅ (release train executed under the lifted no-commit directive; standing rule R8 kept: no human step performed or faked) | evidence: (1) LIVE FRESH: :8000 restarted on current tree -> {"status":"ok","version":"0.5.0","env":"development"}; :4174 vite preview on freshly built dist. (2) USER-MODE BROWSER E2E (in-app browser, product flows only, all roles): search-hit -> chapter reader; theme dark<->light + bn<->en persistence; tutor stream renders single bubble; quiz start->grade (student home progress 20%->30% after 2nd quiz, correct average); revision mini-quiz 5Q -> 40% + explain deep-links; Me page invite code (1440-min) + data export + dark/low-data switches; parent register -> redeem invite -> per-chapter accuracy + weak flags; teacher CSV import (created x2 + duplicate_email path) -> per-student quiz assign -> attempt graded -> QPaper draft->review->finalize -> paper+answer PDFs valid %PDF (11654/12316 bytes); admin: stats 134 users, safety refusals (insufficient_evidence 116 / sexual_content 4 / self_harm 4), feedback triage Open 1->0, audit qp_finalize row, k-anon govt export (min_cell=5, suppressed_cells=2, pii_checked=true), impersonation start 200/15min -> student-scoped /users/me -> exit 204 -> token revoked, public /status green; logout + delete-account UI (redirect + 401 on relogin). QA accounts deleted via product flows (204 + 401-verify); sweep of teacher-import artifacts + orphan checks clean -> users back to 129 baseline; retention-purge button never clicked (standing directive). (3) BUGS FOUND+FIXED: BUG-1 tutor stream final answer rendered as a SECOND bubble (stream placeholder id 0 never matched the finalized message_id -> finalize merges the trailing bubble; regression test tutorStreamDedup.test.tsx). BUG-2 Me-page data export was an unauthenticated <a href> -> 401 Not authenticated (auth is header-based; replaced with authed fetch + blob download; regression test dataExport.test.tsx x2). BUG-3 RELEASE-BLOCKING, caught by the docker gate and invisible to pytest (which imports from src/): the wheel shipped only data/sample_nctb/*.md, so the ENV=ci container crashed at boot FileNotFoundError data/kg/prerequisites.json (the PDF font would have shipped missing too) -> pyproject package-data extended with data/kg/*.json + data/fonts/*.ttf; image rebuilt -> /health status:ok + /ready provider:mock + both files verified inside site-packages. BUG-4 RELEASE-BLOCKING, caught by GitHub CI (Postgres engine check) and INVISIBLE to local sqlite pytest: DELETE /users/me violated daily_activity_student_id_fkey on Postgres — delete_me never covered the S1.9+ child tables (12 FKs -> students.id incl. daily_activity/chapter_progress/revision_item/class_student/student_invite/support_plan/concept_mastery/student_ability, plus ClassTeacher->teachers, teacher artifacts QuestionPaper/ShortTest/Assignment/QuestionBankEntry, and 14 refs -> users.id). SQLite silently ignores FKs, so only the CI Postgres service caught it. Fix: delete_me is now FK-complete — children deleted, shared/audit rows anonymised instead of destroyed (AuditLog.actor_user_id -> NULL, ChapterContent.created_by -> NULL, SchoolInvite.used_by -> NULL, redeemed ParentInvite.used_by_parent_id -> NULL), plus new tests/test_delete_cascade_v2.py x3 (student/teacher/parent paths, FK-enforced assertions on sqlite). FULL pytest now 492 passed + 6 skipped (R6: +3 new coverage, no removes); re-verified green against a throwaway Postgres 16 container (test_postgres_smoke 1 passed) before re-push; AuditLog docstring updated to record the single documented erasure-anonymisation exception. (4) INVESTIGATION NOTE: parent dashboard briefly showed an unrelated child — traced to an orphan parent_student_links row from a PRIOR session's raw-SQL user deletion + SQLite rowid reuse; product delete_me cleans both sides (main.py ~1697/1719), NOT a product bug; stale row removed. (Honest correction added after BUG-4: this note was right about the parent-link pair but delete_me's overall cleanup was in fact incomplete — see BUG-4.) (5) LOCAL CI MATRIX: ruff check + format --check clean (6 files reformatted — unpushed S5/S6 code had never seen the CI-enforced formatter), mypy 76 files, alembic upgrade->downgrade->upgrade on throwaway sqlite (head a1b2c3d4e5f6), pip-audit 0 vulns, npm audit --omit=dev 0, vitest 92/92 (+3 new this phase), golden eval 15 items hit@3 1.0 / grounded 0.9333, FULL pytest 489 passed + 6 skipped exit 0. R6 note (honest): suite total 495 unchanged vs 492+3 — the 3 delta-skips are the pre-existing REDIS_URL skipif gates in test_cache_v2.py (never run in CI either — no redis service); PROVEN green by running that file against a throwaway REAL Redis 7 container: 19/19 passed, container removed. (6) CONCURRENCY DRILL v0.5.0 (throwaway sqlite copy probe_tmp.db, port 8030, LLM_PROVIDER=mock — dev.db and real Gemini untouched): 1c 0% err p95 18ms; 10c kept under the 30/min/user tutor cap 0% err p95 153ms (~65 RPS); the apparent 65-70% "errors" at 5-10c/40req were the per-user rate limiter shedding 429s BY DESIGN (cross-checked against rate_limit_tutor_per_minute=30). Cleanup: server killed, probe db deleted, live stack verified intact. (7) REPO HYGIENE for the commit: gitignore additions for apps/api/fonts/ scratch (root-anchored /fonts/ so the runtime font stays tracked) + apps/web android|ios Capacitor scaffolds (npx cap add android regenerates); PWA icons icon-192/512.png (manifest-referenced) + runtime data/fonts/*.ttf + data/kg/*.json ARE committed; golden_report.json stays gitignored (regenerable). | commit: v0.5.0 release train
- [ ] **6.1 NCTB corpus pipeline** | status: todo | evidence: — | commit: — | 🖐 NCTB PDFs
- [x] ~~**6.2 Content QA**~~ | superseded stale duplicate row (completed above as S6.2)
- [ ] **6.3 AI Classroom v1** | status: todo | evidence: — | commit: —
- [ ] **6.4 School analytics v2 + term report** | status: todo | evidence: — | commit: —
- [x] ~~**6.5 Govt/authority reporting**~~ | superseded stale duplicate row (aggregate export completed above as S6.5)
- [x] ~~**6.6 Capacity plan**~~ | superseded stale duplicate row (completed above as S6.6)
- [~] ~~**6.7 Hosting decision**~~ | stale duplicate row — see S6.7 entry above: drafted, ✍ sign-off still PENDING
- [x] ~~**6.8 Load + chaos game-day**~~ | superseded stale duplicate row (local legs completed above as S6.8; staging legs stay ✍)
- [ ] **7.1 Pilot** | status: todo | evidence: — | commit: — | 🖐 Schools
- [ ] **7.2 Teacher training kit** | status: todo | evidence: — | commit: —
- [ ] **7.3 Release train + graduation** | status: todo | evidence: — | commit: —

## GATE G5 (enterprise gate) — local verdict: PASS with documented human-input pendings

- security: pen-test-lite redteam 16/16 refusals (G4); audit_log covers 5 privileged events incl. impersonation start/exit (live audit rows → S5.10); CSP + security headers in middleware (post-build validation round); npm audit --omit=dev 0 + SAST/supply-chain gates in CI; PII Fernet-encrypted (parents.phone_enc) with owner-only decrypt (S5.6). PASS
- reliability: zero-downtime rolling deploy demoed locally (scripts/deploy_rolling.py, S5.2); rollback procedure + restore drill PASS (S5.7 local); DR doc present. PASS local; staging rerun = 🖐 G3 pending
- observability: Sentry wired (no-op without DSN), Prometheus+Grafana stack with alert rules, request_id on every response/log line. PASS
- scalability: k6 + concurrency probe thresholds (S5.5 local); DB pooling configured; stateless replicas proven via Redis-backed rate limits + shared cache (S5.3) which S5.10 impersonation revocation now also relies on. PASS local
- quality: pytest 470 passed + 3 skipped (gate >=200, never regressed R6); eval gate grounded 1.0 (G4); deploy smoke green. PASS
- compliance: guardian consent protected + reconfirm flow w/ version bump (S5.8), retention sweep w/ dry-run evidence + nightly job (S5.8), data-minimization review + DPA template docs. PASS local; Bangladesh legal review = 🖐 PENDING (never faked)
- ops: docs/runbook.md (incl. new Support ops & SLA section), public status page live on API+web (S5.10), support SLA documented (Sev-1 1d / Sev-2 3d). PASS

**G5 verdict: PASS (local).** Carried human-input items: 🖐 staging environment, 🖐 legal DPA review, 🖐 Android keystore/Play, 🖐 FCM project, 🖐 NCTB files + permission. All evidence local-only unless marked; nothing committed or pushed per standing directive. | commit: — (no-commit directive)

## Blockers
**B-2026-09-08 (Stage 6 entry):** S6.1 NCTB corpus pipeline is gated on human input per spec (PDF->extract->bijoy->unicode->chunk->quality-gate->index PER EDITION requires the ACTUAL NCTB textbook PDFs and written permission/license tracking; a synthetic stand-in would fake the step's PASS-WHEN 'sample batch end-to-end' against real editions). Stages 6 and 7 depend on this step (R1 strict order) plus further human inputs (staging, schools/recruitment, hosting sign-off, Play account). ASK: (1) provide licensed NCTB PDFs + permission record to unblock 6.1; (2) confirm whether to proceed on the non-NCTB-dependent local steps (6.2 QA checks, 6.5 PII-zero export, 6.6 capacity doc, 6.7 hosting doc, 6.8 local degraded-mode drill) while waiting. Everything through GATE G5 is complete and verified locally.

- None yet

## Evidence Log
- 2026-09-04: Branch created, PROGRESS initialized at S0.1
- 2026-09-08: Phase H v0.5.0 — live all-role user-mode E2E pass, BUG-1/2/3 fixed, local CI matrix green (489+6, docker smoke ok after package-data fix), no-commit directive lifted, release train (commit -> main FF -> CI -> tag v0.5.0 -> GHCR) executed; report docs/FINAL_CICD_EXECUTION_REPORT_v0.5.0.md
- 2026-09-08: Phase H BUG-4 — GitHub CI Postgres engine check caught FK-incomplete DELETE /users/me (daily_activity_student_id_fkey; sqlite never enforces FKs). delete_me made FK-complete (children deleted, audit/shared rows anonymised), +3 regression tests, full suite 492+6, re-proven vs throwaway postgres:16; CI re-run pending re-green before tag
